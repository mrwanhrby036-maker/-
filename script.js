/* #####################################################################
   منصتي الرقمية — ملف الجافا سكريبت (ملف واحد)
     الجزء ١: الإعدادات والمحتوى (الأقسام ← الوحدات ← الدروس) ← بيتعدل
     الجزء ٢: بنك الأسئلة (الامتحان النهائي لكل درس)          ← بيتعدل
     الجزء ٣: برمجة المنصة                                   ← مش محتاج يتعدل
   ##################################################################### */


/* =====================================================================
   الجزء ١: الإعدادات والمحتوى
   قسم التاريخ: الدرس الأول من كتاب الوزارة «التاريخ الوطني» — قسم اللغة العربية لسه فاضي
   ===================================================================== */
const SITE = {
  name: "منصتي الرقمية",
  tagline: "تعلّم المادة بطريقة تفاعلية ومنظمة",
  competition: "مسابقة «مصر الرقمية» — المرحلة الثانوية ٢٠٢٦",
  subject: "التاريخ",
  grade: "الصف الثاني الثانوي",

  // شخصيات الحوار
  characters: {
    teacher: { name: "المرشد" },
    student: { name: "الطالب" },
  },

  student: {
    name: "..............................",
    school: "..............................",
    idara: "..............................",
    governorate: "أسيوط",
    teacher: "..............................",
  },

};

/* ---------------------------------------------------------------------
   شكل الدرس:
   {
     id, title, summary,
     objectives: ["..."],                         ← أهداف الدرس
     parts: [                                     ← أجزاء صغيرة
       {
         title: "عنوان الجزء",
         dialogue: [                              ← الحوار
           { who: "teacher", text: "..." },       ← المرشد
           { who: "student", text: "..." },       ← الطالب
         ],
         example: { title: "تشبيه", text: "..." },   ← (اختياري) مثال أو تشبيه
                  (أو قائمة أمثلة؛ كل مثال ممكن يكون text أو quote أو table أو img + caption لصورة من فولدر images)
         task: {                                  ← (اختياري) التطبيق العملي
           steps: ["...", "..."],                 ← خطوات مرقمة
           lang: "html" | "js" | "css" | "text",  ← html/js/css بيتشغّلوا جوه المنصة
           code: "الكود المبدئي",
           hints: ["تلميح ١", "تلميح ٢"],
           check: ["كلمة لازم تكون في الحل"],     ← (اختياري) للتحقق التلقائي
         },
         checkpoint: [ أسئلة تحقق بنفس شكل أسئلة البنك ],
       },
     ],
     keypoints: ["..."],                          ← ملخص الدرس
   }
--------------------------------------------------------------------- */
/* المنصة متقسمة أقسام حسب المادة: كل قسم جواه وحدات، وكل وحدة جواها دروس */
const SUBJECTS = [
  /* ================= التاريخ ================= */
  {
    id: "history", title: "التاريخ", icon: "pyramid", desc: "الوحدة الأولى: مصر بين الماضي والحاضر",
    units: [
  {
    id: "u1", title: "الوحدة الأولى: مصر بين الماضي والحاضر", desc: "مصر بين الماضي والحاضر",
    lessons: [
      /* ================= الدرس الأول ================= */
      {
        id: "u1l1",
        title: "بناء الدولة المصرية واستمرارها عبر التاريخ",
        summary: "مفهوم الدولة وأركانها، ومقومات نشأة الدولة المصرية وعوامل استمرارها عبر الزمن",
        objectives: [
          "تعرّف مفهوم الدولة، وتستنتج أركانها من الدستور المصري.",
          "توضح أدوار الشعب في بناء الدولة وتنميتها وحمايتها.",
          "تحدد مكونات الحيز الجغرافي لمصر وحدودها.",
          "تميّز بين اختصاصات السلطات الثلاث وفق مواد الدستور.",
          "تفرّق بين السيادة على الشئون الداخلية والسيادة على الشئون الخارجية.",
          "تشرح مقومات نشأة الدولة المصرية وعوامل استمرارها عبر الزمن.",
          "تعتز بهويتك المصرية، وتقدّر دورك بصفتك مواطنًا مسئولًا.",
        ],
        parts: [
          /* ---------- الجزء ١ ---------- */
          {
            title: "مفهوم الدولة وأركانها",
            dialogue: [
              { who: "teacher", text: "أهلًا بك! سنبدأ رحلتنا اليوم بسؤال بسيط: ما الذي يجعل مصر «دولة»؟" },
              { who: "student", text: "لأن لها أرضًا وحدودًا على الخريطة؟" },
              { who: "teacher", text: "إجابة جيدة، لكنها نصف الحقيقة! الدولة وحدة سياسية تشغل حيزًا جغرافيًا محددًا، وتضم شعبًا يقيم إقامة دائمة ويخضع لقوانينها، وتديرها سلطة منظمة تباشر اختصاصاتها بما يحقق الأمن والاستقرار، وتمارس سيادتها على شئونها الداخلية والخارجية." },
              { who: "student", text: "تعريف طويل! هل يمكن أن نقسمه إلى عناصر حتى يسهل حفظه؟" },
              { who: "teacher", text: "فكرة رائعة! تقسيم النص إلى عناصر مهارة مهمة. من هذا التعريف نستخرج أربعة أركان: الشعب، والحيز الجغرافي، والسلطة، والسيادة." },
              { who: "student", text: "أفهم! لكن ماذا لو غاب ركن واحد فقط؟" },
              { who: "teacher", text: "سؤال ممتاز! الأركان مترابطة؛ إذا اختل أحدها تأثر كيان الدولة بأكمله. فالشعب بدون أرض يصير شتاتًا، والأرض بدون سلطة تصير فوضى، والسلطة بدون سيادة مجرد إدارة تحت الوصاية." },
              { who: "student", text: "وهل ينص الدستور المصري على هذه الأركان؟" },
              { who: "teacher", text: "نعم، اقرأ المادة الأولى من دستور ٢٠١٤م بالأسفل، وستجد أن مصر تمتلك الأركان الأربعة كاملة." },
            ],
            example: [
              { title: "من الدستور المصري ٢٠١٤م", source: "المادة الأولى — دستور جمهورية مصر العربية ٢٠١٤م",
                quote: "جمهورية مصر العربية دولة ذات سيادة، موحدة لا تقبل التجزئة، ولا ينزل عن شيء منها، نظامها جمهوري ديمقراطي، يقوم على أساس المواطنة وسيادة القانون. الشعب المصري جزء من الأمة العربية يعمل على تكاملها ووحدتها، ومصر جزء من العالم الإسلامي، تنتمي إلى القارة الإفريقية، وتعتز بامتدادها الآسيوي، وتسهم في بناء الحضارة الإنسانية" },
              { title: "نستنتج من النص: أركان الدولة في مصر",
                table: { head: ["الركن", "في مصر"], rows: [
                  ["١) الشعب", "المواطنون المصريون"],
                  ["٢) الحيز الجغرافي", "حيز جغرافي بحدود معترف بها"],
                  ["٣) السلطة", "سلطات تشريعية وتنفيذية وقضائية تدير شئونها الداخلية والخارجية"],
                  ["٤) السيادة", "تتمتع مصر بالسيادة"],
                ] } },
              { title: "تشبيه يقرّب الفكرة",
                text: "كما تسير أمور الحياة اليومية والعلاقات الأسرية وفق قواعد وعادات وتقاليد متفق عليها، كذلك تسير الدولة المصرية وفق قواعد حاكمة لتنظيم شئونها العامة من خلال المبادئ الرئيسية التي يرسيها الدستور." },
            ],
            task: {
              steps: [
                "اقرأ تعريف الدولة في الحوار مرة أخرى.",
                "قسّم التعريف إلى عناصر: من يسكن؟ أين؟ من يدير؟ هل يخضع لسلطة خارجية؟",
                "اكتب في المربع أركان الدولة الأربعة، ثم اضغط «تحقّق من إجابتك».",
              ],
              lang: "text",
              placeholder: "اكتب أركان الدولة الأربعة هنا...",
              hints: ["من يسكن الدولة؟ ← ركن … وأين يسكنون؟ ← ركن …", "من يدير شئونهم؟ ← ركن … واستقلال قرارهم عن أي سلطة خارجية ← ركن …"],
              check: ["الشعب", "الحيز الجغرافي", "السلطة", "السيادة"],
              model: "أركان الدولة: الشعب – الحيز الجغرافي – السلطة – السيادة.",
            },
            checkpoint: [
              { type: "tf", q: "إذا اختل ركن واحد من أركان الدولة فلا يتأثر كيان الدولة.", answer: false, explain: "الأركان مترابطة؛ إذا اختل أحدها تأثر كيان الدولة بأكمله." },
              { type: "mcq", q: "أي مما يلي ليس من أركان الدولة؟", options: ["الموارد الطبيعية", "الشعب", "السيادة", "السلطة"], answer: 0, explain: "الموارد الطبيعية من مقومات نشأة الدولة المصرية، وليست من أركانها." },
              { type: "mcq", q: "تنص المادة الأولى من الدستور المصري على أن جمهورية مصر العربية دولة ذات سيادة، موحدة ..........", options: ["لا تقبل التجزئة", "تقبل التنازل عن جزء منها", "نظامها ملكي", "لا تنتمي إلى القارة الإفريقية"], answer: 0, explain: "«دولة ذات سيادة، موحدة لا تقبل التجزئة، ولا ينزل عن شيء منها»." },
            ],
          },

          /* ---------- الجزء ٢ ---------- */
          {
            title: "الركن الأول: الشعب",
            dialogue: [
              { who: "teacher", text: "لنبدأ بأول ركن: الشعب. يشمل جميع المواطنين المقيمين داخل حدود الدولة أو خارجها ممن يحملون الجنسية المصرية." },
              { who: "student", text: "يعني المصري الذي يعمل خارج مصر جزء من الشعب أيضًا؟" },
              { who: "teacher", text: "تمامًا! ووفقًا للدستور المصري في مادته الرابعة: السيادة للشعب وحده، يمارسها ويحميها، وهو مصدر السلطات." },
              { who: "student", text: "هل يكفي أن يكون عدد الشعب كبيرًا لتصبح الدولة قوية؟" },
              { who: "teacher", text: "لا، دور الشعب لا يقتصر على الوجود العددي، بل يمتد إلى المشاركة الفعالة في بناء الدولة وتنميتها وحمايتها. وكلما كان الشعب واعيًا بحقوقه وواجباته كانت الدولة أقوى وأكثر تقدمًا." },
              { who: "student", text: "وما هذه الأدوار بالتحديد؟" },
              { who: "teacher", text: "خمسة أدوار: دعم أمن الدولة واستقرارها، والإسهام في التنمية الاقتصادية، والحفاظ على هوية المجتمع، والمشاركة السياسية، والدفاع عن الوطن. ستجدها مفصلة في الجدول بالأسفل." },
              { who: "student", text: "هل هناك مثال من التاريخ يوضح دور الشعب؟" },
              { who: "teacher", text: "بالتأكيد: ثورة ١٩١٩م بقيادة الزعيم سعد زغلول؛ رفض فيها الشعب الاحتلال البريطاني، وشاركت فيها مختلف فئات المجتمع في مشهد جسّد وحدة المصريين حول هدف الاستقلال." },
              { who: "student", text: "لقد فهمت! الشعوب تستطيع إحداث التغيير عندما تتحد حول قضية وطنية." },
            ],
            example: [
              { title: "أدوار الشعب",
                table: { head: ["الدور", "من خلال"], rows: [
                  ["دعم أمن الدولة واستقرارها", "الالتزام بالدستور والقوانين المختلفة واحترامها."],
                  ["الإسهام في تحقيق التنمية الاقتصادية", "العمل والإنتاج في مختلف القطاعات، ودفع الضرائب التي تموّل المشروعات العامة."],
                  ["الحفاظ على هوية المجتمع", "الحفاظ على التراث المادي والثقافي، واحترام عادات المجتمع وتقاليده."],
                  ["المشاركة السياسية", "اختيار الحكام وممثلي الشعب عبر الانتخابات، ومراقبة أداء الحكومة من خلال ممثليهم في مجلس النواب."],
                  ["الدفاع عن الوطن", "مساندة القوات المسلحة ودعمها معنويًا، والمشاركة في الأعمال التطوعية وقت الأزمات والكوارث."],
                ] } },
              { title: "ثورة ١٩١٩م ودور الشعب", source: "من كتاب الوزارة",
                quote: "الشعب المصري هو القوة المحركة للتغيير في المجتمع، وقد برهن على ذلك عبر محطات تاريخية عديدة، كان أبرزها ثورة ١٩١٩م بقيادة الزعيم سعد زغلول؛ فقد جاءت الثورة تعبيرًا عن رفض الشعب للاحتلال البريطاني، وشارك فيها مختلف فئات المجتمع، في مشهد جسّد وحدة المصريين حول هدف الاستقلال، وأسهمت هذه المشاركة الشعبية في تعزيز الوعي الوطني، وترسيخ قيم المواطنة، وأجبرت سلطات الاحتلال على إعادة النظر في سياساتها" },
            ],
            task: {
              steps: [
                "اقرأ جدول «أدوار الشعب».",
                "فكّر في دورك أنت بصفتك طالبًا ومواطنًا مصريًا.",
                "اكتب ثلاثة أدوار يمكنك القيام بها، مع ذكر مثال لكل دور.",
              ],
              lang: "text",
              placeholder: "حدد دورك بصفتك مواطنًا مسئولًا في الدولة المصرية...",
              hints: ["الالتزام بالقوانين يدعم أمن الدولة واستقرارها.", "المشاركة في الأعمال التطوعية وقت الأزمات من صور الدفاع عن الوطن."],
              check: [],
              model: "المشاركة السياسية: اختيار الحكام وممثلي الشعب عبر الانتخابات.\nدعم أمن الدولة واستقرارها: الالتزام بالدستور والقوانين المختلفة واحترامها.\nالإسهام في التنمية الاقتصادية: العمل والإنتاج في مختلف القطاعات، ودفع الضرائب.\nالحفاظ على هوية المجتمع: الحفاظ على التراث المادي والثقافي، واحترام عادات المجتمع وتقاليده.\nالدفاع عن الوطن: مساندة القوات المسلحة ودعمها معنويًا، والمشاركة في الأعمال التطوعية وقت الأزمات والكوارث.",
            },
            checkpoint: [
              { type: "tf", q: "يقتصر الشعب على المواطنين المقيمين داخل حدود الدولة فقط.", answer: false, explain: "يشمل الشعب جميع المواطنين المقيمين داخل حدود الدولة أو خارجها ممن يحملون الجنسية المصرية." },
              { type: "mcq", q: "دفع الضرائب التي تموّل المشروعات العامة من صور ..........", options: ["الإسهام في تحقيق التنمية الاقتصادية", "المشاركة السياسية", "الدفاع عن الوطن", "الحفاظ على هوية المجتمع"], answer: 0, explain: "الإسهام في التنمية الاقتصادية يكون بالعمل والإنتاج ودفع الضرائب." },
              { type: "mcq", q: "يكمن الأثر السياسي المباشر لمشاركة جميع أطياف الشعب في ثورة ١٩١٩م في ..........", options: ["ترسيخ مبدأ المواطنة وتعزيز الوعي الوطني", "تغيير طبيعة ونوع النظام الاقتصادي والإنتاجي للدولة", "التنازل عن ركن السلطة لصالح اللجان الشعبية المؤقتة", "الاقتصار على حماية الحدود التابعة للحيز الجغرافي"], answer: 0, explain: "أسهمت المشاركة الشعبية في تعزيز الوعي الوطني وترسيخ قيم المواطنة." },
            ],
          },

          /* ---------- الجزء ٣ ---------- */
          {
            title: "الركن الثاني: الحيز الجغرافي",
            dialogue: [
              { who: "teacher", text: "الركن الثاني هو الحيز الجغرافي: الرقعة المحددة التي تمارس الدولة سيادتها عليها، ولا تقوم دولة من دون حدود محددة معترف بها." },
              { who: "student", text: "الرقعة؟ تقصد الأرض فقط؟" },
              { who: "teacher", text: "هذا خطأ شائع! الحيز الجغرافي يشمل ثلاثة عناصر: اليابس، والمياه الإقليمية، والمجال الجوي." },
              { who: "student", text: "لنبدأ باليابس إذن. أين تقع حدود مصر؟" },
              { who: "teacher", text: "اليابس هو المساحة الأرضية التي يعيش عليها السكان داخل إطار الحدود السياسية. حدود مصر: شمالًا ساحل البحر المتوسط، وجنوبًا دائرة عرض ٢٢ درجة شمالًا، وشرقًا ساحل البحر الأحمر وخليج العقبة والحدود البرية مع فلسطين، وغربًا خط طول ٢٥ درجة شرقًا مع ليبيا." },
              { who: "student", text: "وماذا عن البحر؟ هل البحر المتوسط كله تابع لمصر؟" },
              { who: "teacher", text: "لا، المياه الإقليمية هي المساحات البحرية التي تمتد عادة لمسافة ١٢ ميلًا بحريًا من خط الأساس للساحل. وقد عيّنت مصر حدودها البحرية في البحر المتوسط بدقة للحفاظ على ثرواتها." },
              { who: "student", text: "والسماء أيضًا تابعة للدولة؟" },
              { who: "teacher", text: "أحسنت! المجال الجوي هو المساحة الرأسية التي تعلو اليابس والمياه الإقليمية، وتمارس الدولة سيادتها عليه؛ لذلك لا يُسمح لأي طائرة بدخول المجال الجوي المصري من دون تصريح رسمي." },
            ],
            example: [
              { title: "حدود مصر (اليابس)",
                table: { head: ["الحدود", "التفاصيل"], rows: [
                  ["الشمالية", "عند خط ساحل البحر المتوسط."],
                  ["الجنوبية", "عند دائرة عرض ٢٢ درجة شمالًا، والتي رُسمت عام ١٨٩٩م لتفصل مصر عن السودان."],
                  ["الشرقية", "حدود بحرية: خط ساحل البحر الأحمر وخليج العقبة. حدود برية: خط الحدود بين مصر وفلسطين، الذي رُسم عام ١٩٠٦م."],
                  ["الغربية", "عند خط طول ٢٥ درجة شرقًا، وتمثل الحدود السياسية الفاصلة بين مصر وليبيا، والتي رُسمت عام ١٩٢٥م."],
                ] } },
              { title: "حدود مصر ليست مجرد خطوط", source: "من كتاب الوزارة",
                quote: "لم تكن حدود مصر يومًا مجرد خطوط على الخريطة، بل كانت رمزًا لوحدة الوطن واستقراره، وشاهدًا على عراقة دولته. وتواصل الدولة أداء دورها في حماية حدودها البرية والبحرية والجوية، والدفاع عن سيادتها؛ لتظل مصر آمنة ومستقرة وقادرة على مواصلة مسيرتها نحو التنمية" },
            ],
            task: {
              steps: [
                "راجع ما قاله المرشد عن الحيز الجغرافي.",
                "اكتب مكونات الحيز الجغرافي الثلاثة.",
                "اكتب بجوار كل مكون تعريفه باختصار، ثم اضغط «تحقّق من إجابتك».",
              ],
              lang: "text",
              placeholder: "مكونات الحيز الجغرافي هي...",
              hints: ["فكّر في: الأرض، ثم البحر، ثم السماء.", "المساحة البحرية تمتد ١٢ ميلًا بحريًا، والمساحة الرأسية تعلو اليابس والمياه."],
              check: ["اليابس", "المياه الإقليمية", "المجال الجوي"],
              model: "اليابس: المساحة الأرضية التي يعيش عليها السكان داخل إطار الحدود السياسية للدولة.\nالمياه الإقليمية: المساحات البحرية التي تمتد عادة لمسافة ١٢ ميلًا بحريًا من خط الأساس للساحل.\nالمجال الجوي: المساحة الرأسية التي تعلو الحيز الجغرافي للدولة (اليابس، والمياه الإقليمية).",
            },
            checkpoint: [
              { type: "mcq", q: "منع طائرة أجنبية غير مصرح لها بالطيران فوق السماء المصرية، وحماية حقول الغاز في المياه الإقليمية المصرية، إجراءان يرتبطان بركن واحد من أركان الدولة هو ..........", options: ["الحيز الجغرافي للدولة", "النفوذ الاقتصادي الإقليمي", "الحدود الطبيعية الواقية", "التواجد السكاني المتجانس"], answer: 0, explain: "الحيز الجغرافي يشمل اليابس والمياه الإقليمية والمجال الجوي." },
              { type: "mcq", q: "تتمثل الحدود الجنوبية لمصر في ..........", options: ["دائرة عرض ٢٢ درجة شمالًا", "خط طول ٢٥ درجة شرقًا", "خط ساحل البحر المتوسط", "خليج العقبة"], answer: 0, explain: "رُسمت عام ١٨٩٩م لتفصل مصر عن السودان." },
              { type: "tf", q: "تمتد المياه الإقليمية عادة لمسافة ١٢ ميلًا بحريًا من خط الأساس للساحل.", answer: true, explain: "" },
            ],
          },

          /* ---------- الجزء ٤ ---------- */
          {
            title: "الركن الثالث: السلطة",
            dialogue: [
              { who: "teacher", text: "الركن الثالث هو السلطة: المؤسسات التي تدير شئون الدولة، وتضع القوانين وتعمل على تنفيذها، وتحافظ على الأمن العام." },
              { who: "student", text: "هل السلطة جهة واحدة؟" },
              { who: "teacher", text: "لا، حدد الدستور المصري ثلاث سلطات: التشريعية، والتنفيذية، والقضائية." },
              { who: "student", text: "لنبدأ بالتشريعية." },
              { who: "teacher", text: "وفقًا للمادة (١٠١) يتولى مجلس النواب سلطة التشريع، وإقرار السياسة العامة للدولة، والخطة العامة للتنمية الاقتصادية والاجتماعية، والموازنة العامة للدولة، وممارسة الرقابة على أعمال السلطة التنفيذية." },
              { who: "student", text: "والسلطة التنفيذية؟" },
              { who: "teacher", text: "تنص المادة (١٣٩) على أن رئيس الجمهورية هو رئيس الدولة ورئيس السلطة التنفيذية. وتنص المادة (١٦٣) على أن الحكومة هي الهيئة التنفيذية والإدارية العليا للدولة، وتتكون من رئيس مجلس الوزراء ونوابه والوزراء ونوابهم." },
              { who: "student", text: "والسلطة القضائية؟" },
              { who: "teacher", text: "تنص المادة (١٨٤) على أن السلطة القضائية مستقلة، تتولاها المحاكم على اختلاف أنواعها ودرجاتها، وتصدر أحكامها وفقًا للقانون." },
              { who: "student", text: "أفهم! لكن ماذا يحدث لو غابت السلطة؟" },
              { who: "teacher", text: "بدون السلطة يُخرق القانون، وينفلت الأمن، ويتزعزع الاستقرار السياسي والاجتماعي، وتضيع حقوق الأفراد. لذلك فهي ضرورية للحفاظ على النظام والقانون، وحماية حقوق المواطنين، وتحقيق الاستقرار، وتمكين الدولة من ممارسة سيادتها." },
            ],
            example: [
              { title: "نستنتج: اختصاص كل سلطة",
                table: { head: ["السلطة", "الاختصاص", "تمثل"], rows: [
                  ["التشريعية", "سن القوانين وإقرارها، وإقرار السياسة العامة والموازنة العامة للدولة، وممارسة الرقابة على أعمال السلطة التنفيذية.", "عين الشعب"],
                  ["التنفيذية", "إدارة شئون الدولة.", "يد الدولة"],
                  ["القضائية", "الفصل في القضايا والمنازعات وإصدار الأحكام وفقًا للقانون.", "ميزان العدل"],
                ] } },
              { title: "أهمية السلطة للدولة",
                table: { rows: [
                  ["الحفاظ على النظام والقانون", "حماية حقوق المواطنين"],
                  ["تحقيق الاستقرار السياسي والاجتماعي", "تمكين الدولة من ممارسة سيادتها"],
                ] } },
            ],
            task: {
              steps: [
                "اقرأ العبارة: «تحتكر الحكومة في الدولة المصرية سلطة سن القوانين والتشريعات».",
                "حدد: هل العبارة صحيحة أم غير صحيحة؟",
                "اكتب رأيك مع ذكر الجهة التي تتولى التشريع ورقم المادة الدستورية.",
              ],
              lang: "text",
              placeholder: "العبارة ... لأن ...",
              hints: ["من الذي يتولى سلطة التشريع وفقًا للمادة (١٠١)؟", "ما دور الحكومة وفقًا للمادة (١٦٣)؟"],
              check: ["مجلس النواب"],
              model: "العبارة غير صحيحة؛ لأن مجلس النواب (السلطة التشريعية) هو صاحب الاختصاص الأصيل في سن القوانين والتشريعات وفقًا للمادة (١٠١)، بينما تفيد المادة (١٦٣) بأن الحكومة هي الهيئة التنفيذية والإدارية العليا للدولة التي يقتصر دورها على تنفيذ هذه القوانين وتسيير شئون الدولة، مما يجسد مبدأ الفصل بين السلطات.",
            },
            checkpoint: [
              { type: "mcq", q: "تختص بالفصل في القضايا والمنازعات وإصدار الأحكام وفقًا للقانون ..........", options: ["السلطة القضائية", "السلطة التشريعية", "السلطة التنفيذية", "الحكومة"], answer: 0, explain: "المادة (١٨٤): تتولاها المحاكم على اختلاف أنواعها ودرجاتها." },
              { type: "tf", q: "رئيس الجمهورية هو رئيس الدولة، ورئيس السلطة التنفيذية.", answer: true, explain: "المادة (١٣٩) من الدستور." },
              { type: "mcq", q: "«قام أهالي إحدى المناطق الساحلية بتشكيل لجان شعبية لحماية وتنظيم شئونهم في فترة غياب تام للأجهزة الأمنية». يتضح من العبارة أن هذا المجتمع يمتلك ..........", options: ["ركني الشعب والحيز الجغرافي، بينما يفتقد لركن السلطة المكتملة", "ركني السلطة والسيادة، بينما يفتقد لركن الحيز الجغرافي", "ركن السيادة الخارجية فقط، بينما يفتقد لركني الشعب والأرض", "كافة أركان الدولة الأربعة بشكل تام ومكتمل الشروط"], answer: 0, explain: "الأهالي يمثلون الشعب، والمنطقة الساحلية تمثل الحيز الجغرافي، أما غياب الأجهزة الأمنية فيدل على غياب السلطة الحاكمة المكتملة." },
            ],
          },

          /* ---------- الجزء ٥ ---------- */
          {
            title: "الركن الرابع: السيادة",
            dialogue: [
              { who: "teacher", text: "وصلنا إلى الركن الرابع: السيادة، وتعني استقلال الدولة في اتخاذ قراراتها فيما يتعلق بشئونها الداخلية والخارجية من دون خضوع لسلطة خارجية." },
              { who: "student", text: "وماذا يحدث إذا غابت السيادة؟" },
              { who: "teacher", text: "يؤثر غيابها على قدرة الدولة على اتخاذ قراراتها. وتذكّر ما قلناه: السلطة بدون سيادة مجرد إدارة تحت الوصاية." },
              { who: "student", text: "هل للسيادة أنواع؟" },
              { who: "teacher", text: "نعم، نوعان: سيادة على الشئون الداخلية، مثل حرية سن القوانين وحفظ الأمن. وسيادة على الشئون الخارجية، مثل حرية إبرام المعاهدات وإقامة العلاقات الدبلوماسية. التفاصيل في الجدول بالأسفل." },
              { who: "student", text: "وهل سعت مصر للحفاظ على سيادتها عبر التاريخ؟" },
              { who: "teacher", text: "بالتأكيد! هناك أمثلة كثيرة لسعي مصر إلى استقلال قرارها الوطني، مثل طرد الهكسوس في التاريخ القديم، ومقاومة الاحتلال الإنجليزي في التاريخ الحديث." },
              { who: "student", text: "إذن استقلال القرار الوطني يرتبط بقدرة الدولة على ممارسة سيادتها واستغلال مواردها!" },
              { who: "teacher", text: "خطوة ممتازة! هذا بالضبط هو الاستنتاج الصحيح." },
            ],
            example: [
              { title: "أنواع السيادة",
                table: { head: ["النوع", "وتشمل"], rows: [
                  ["السيادة على الشئون الداخلية", "حرية الدولة في سن القوانين – حفظ الأمن – إدارة شئون المواطنين من دون أي ضغوط خارجية."],
                  ["السيادة على الشئون الخارجية", "استقلال الدولة في علاقاتها الدولية – حريتها في إبرام المعاهدات – إقامة العلاقات الدبلوماسية – حماية المصالح الوطنية – التمثيل في المنظمات الدولية."],
                ] } },
              { title: "كلمات للتاريخ", source: "الرئيس عبد الفتاح السيسي — من خطابه في الذكرى الثالثة والسبعين لثورة ٢٣ يوليو ١٩٥٢م",
                quote: "إن مصر ستظل منيعة بجبهتها الداخلية المتماسكة، عصية على المؤامرات والفتن، مبصرة بكل ما يُحاك حولها" },
            ],
            task: {
              steps: [
                "اقرأ العبارة (أ): «لا يُسمح لأي طائرة بدخول المجال الجوي المصري من دون تصريح رسمي من الدولة».",
                "اقرأ العبارة (ب): «الدولة مسئولة عن حماية قناة السويس ومراقبة السفن العابرة للتأكد من سلامة الملاحة».",
                "اكتب علامَ تدل كل عبارة في ضوء دراستك لأركان الدولة.",
              ],
              lang: "text",
              placeholder: "(أ) تدل على ...\n(ب) تدل على ...",
              hints: ["العبارتان مرتبطتان باستقلال قرار الدولة.", "العبارة (أ) مرتبطة أيضًا بأحد مكونات الحيز الجغرافي."],
              check: ["سيادة"],
              model: "(أ) تدل على ممارسة الدولة لسيادتها الكاملة على مجالها الجوي بوصفه جزءًا من حيزها الجغرافي، لحماية أمنها القومي.\n(ب) تدل على سيادة الدولة على مرافقها الحيوية وإدارة شئونها الداخلية والخارجية.",
            },
            checkpoint: [
              { type: "mcq", q: "حرية الدولة في إبرام المعاهدات من صور ..........", options: ["السيادة على الشئون الخارجية", "السيادة على الشئون الداخلية", "السلطة القضائية", "الحيز الجغرافي"], answer: 0, explain: "" },
              { type: "tf", q: "حفظ الأمن من صور السيادة على الشئون الداخلية.", answer: true, explain: "تشمل السيادة الداخلية: حرية سن القوانين، وحفظ الأمن، وإدارة شئون المواطنين." },
              { type: "mcq", q: "يمكن تفسير الارتباط الوثيق بين مبدأ «السيادة الوطنية» وتحقيق التنمية الشاملة بأن ..........", options: ["السيادة تضمن استقلالية إدارة الموارد", "التنمية تعوض نقص الحيز الجغرافي", "التنمية تقتصر على المقوم البشري", "السيادة تلغي دور السلطة التشريعية"], answer: 0, explain: "امتلاك الدولة لسيادتها يمنحها حرية إدارة مواردها دون خضوع لضغوط خارجية، وهو شرط أساسي للتنمية." },
            ],
          },

          /* ---------- الجزء ٦ ---------- */
          {
            title: "مقومات نشأة الدولة المصرية (١): الموقع والموارد",
            dialogue: [
              { who: "teacher", text: "رائع! انتهينا من الأركان. والآن سؤال جديد: كيف نشأت الدولة المصرية أصلًا؟" },
              { who: "student", text: "هل هناك عوامل ساعدت على نشأتها؟" },
              { who: "teacher", text: "نعم، تنشأ الدولة نتيجة تضافر مقومات تحول المجتمع من تجمعات بشرية مشتتة إلى كيان سياسي منظم، وهي ستة: الموقع الجغرافي والحدود الطبيعية، والموارد الطبيعية، والتجانس البشري والوحدة الوطنية، والحكومة المركزية القوية، والقوة العسكرية، والحفاظ على الهوية المصرية." },
              { who: "student", text: "لنبدأ بالموقع الجغرافي." },
              { who: "teacher", text: "وقوع مصر في قلب قارات العالم القديم جعلها مركزًا مهمًا للتواصل الحضاري والتبادل التجاري بين الشعوب. أما حدودها الطبيعية فكانت حواجز حمت قلب الدولة: الوادي والدلتا." },
              { who: "student", text: "وما فائدة هذه الحماية؟" },
              { who: "teacher", text: "ساعدت على استقرار المجتمع المصري، فركّز المصريون على الزراعة والعمران وتنظيم شئون حياتهم، وهو ما دعم نشأة الدولة واستقرارها عبر العصور. ولهذا قال جمال حمدان: مصر موضعًا دولة نهر، وموقعًا دولة برزخ." },
              { who: "student", text: "دولة برزخ؟ ماذا تعني؟" },
              { who: "teacher", text: "هي الدولة التي تقع في موقع يربط بين كتلتين يابستين كبيرتين، أو يفصل بين بحرين مهمين عبر شريط أرضي ضيق. فمصر ملتقى بين إفريقيا وآسيا وأوروبا، وبين البحر المتوسط والبحر الأحمر." },
              { who: "student", text: "وماذا عن النيل؟" },
              { who: "teacher", text: "النيل أهم مورد طبيعي في مصر؛ بسببه تحول المصري القديم من «جامع للثمار» إلى «مزارع مستقر»، وتطلب ذلك تنظيم الري وبناء السدود والعمل الجماعي ووجود سلطة مركزية قوية تنظم توزيع المياه وتحل النزاعات بين الأقاليم. كما كان شريان اتصال يربط الشمال بالجنوب." },
              { who: "student", text: "وهل توجد موارد أخرى غير النيل؟" },
              { who: "teacher", text: "نعم: الأراضي الخصبة، والمعادن، وأحجار البناء؛ وقد أسهمت في تنشيط الزراعة والصناعة والتجارة، وتوفير مقومات الاستقرار الاقتصادي." },
            ],
            example: [
              { title: "الموقع الجغرافي والحدود الطبيعية",
                table: { head: ["أهمية الموقع الجغرافي", "دور الحدود الطبيعية"], rows: [
                  ["وقوع مصر في قلب قارات العالم القديم جعلها مركزًا مهمًا للتواصل الحضاري، والتبادل التجاري بين الشعوب.", "مثّلت حواجز طبيعية قامت بدور حماية قلب الدولة (الوادي والدلتا)."],
                ] } },
              { title: "قال جمال حمدان في كتابه «شخصية مصر»", source: "د. جمال حمدان — شخصية مصر: دراسة في عبقرية المكان",
                quote: "في مصر بالذات لن تفهم كيانها أو تاريخها بشكل صحيح خارج إطار الموقع وبغير الإشارة إليه؛ فمصر موضعًا دولة نهر، وموقعًا دولة برزخ، ولو لم يكن النيل فرضًا لفرض الموقع نفسه يقينًا" },
              { title: "أنشودة النيل", source: "رتّلها المصري القديم تقديسًا لدور النيل في حياته",
                quote: "تحيةً لك يا (حابي) الذي يخرج من الأرض ويصل لكي يعطي الحياة لمصر، أنت الذي أخفى مصادره في الظلمات، أنت الفيضان الذي ينساب على الأرض الخضراء؛ ليمنح الحياة إلى جميع هؤلاء الظمأى، وعندما ترتفع تشدو الأرض كلها فرحًا" },
              { title: "المصري القديم والنيل", source: "من كتاب الموتى",
                quote: "لم ألوث ماء النهر، لم أمنع الماء في موسمه، لم أُقِم سدًّا أمام الماء المتدفق" },
            ],
            task: {
              steps: [
                "تذكّر كيف غيّر نهر النيل حياة المصري القديم.",
                "فكّر: ماذا كان سيحدث لو لم يوجد نهر النيل في مصر؟",
                "اكتب توقعين على الأقل، ثم قارن إجابتك بالإجابة النموذجية.",
              ],
              lang: "text",
              placeholder: "لو لم يوجد نهر النيل في مصر لـ...",
              hints: ["هل كان المصري القديم سيتحول إلى مزارع مستقر؟", "هل كانت هناك حاجة لتنظيم الري وقيام حكومة مركزية؟"],
              check: [],
              model: "استمرار الإنسان المصري «جامعًا للثمار» متنقلًا بدلًا من تحوله إلى «مزارع مستقر».\nعدم الحاجة للعمل الجماعي لبناء السدود وتنظيم الري، وبالتالي عدم قيام إدارة مركزية أو «حكومة» مبكرة.\nتحول معظم أرض مصر إلى صحراء قاحلة، وتعذر قيام الحضارة المصرية القديمة.",
            },
            checkpoint: [
              { type: "tf", q: "بسبب نهر النيل تحول المصري القديم من «مزارع مستقر» إلى «جامع للثمار».", answer: false, explain: "العكس هو الصحيح: تحول من «جامع للثمار» إلى «مزارع مستقر»." },
              { type: "mcq", q: "يقصد جمال حمدان بأن مصر بموقعها «دولة برزخ» أنها ..........", options: ["ملتقى بين قارات العالم القديم وبين البحار المهمة", "دولة تعتمد على نهر واحد في الزراعة", "دولة ليس لها حدود طبيعية", "دولة معزولة عن الشعوب المجاورة"], answer: 0, explain: "مصر ملتقى بين إفريقيا وآسيا وأوروبا، وبين البحر المتوسط والبحر الأحمر." },
              { type: "mcq", q: "قامت الحدود الطبيعية لمصر بدور ..........", options: ["حماية قلب الدولة (الوادي والدلتا)", "رسم الحدود الجنوبية عام ١٨٩٩م", "تنظيم توزيع مياه الري", "إقرار الموازنة العامة للدولة"], answer: 0, explain: "" },
            ],
          },

          /* ---------- الجزء ٧ ---------- */
          {
            title: "مقومات نشأة الدولة المصرية (٢): الشعب والحكومة والجيش والهوية",
            dialogue: [
              { who: "teacher", text: "نكمل المقومات: التجانس البشري والوحدة الوطنية. تعد مصر من أكثر دول العالم تجانسًا؛ حيث انصهر سكانها في بوتقة ثقافية واحدة منذ فجر التاريخ، فتشكّل «اتحاد متماسك» يصعب تفتيته." },
              { who: "student", text: "وما نتيجة هذا التجانس؟" },
              { who: "teacher", text: "جعل الوحدة السياسية أمرًا طبيعيًا، وساعد المصريين على عدم التأثر بثقافة المحتل، فحافظوا على هويتهم المميزة على مر العصور." },
              { who: "student", text: "لكن اللغة في مصر تغيرت عبر التاريخ، أليس كذلك؟" },
              { who: "teacher", text: "ملاحظة ذكية! كانت اللغة المصرية القديمة لغة أهل مصر، ثم تحدثوا القبطية في فترة الحكم الروماني بعد انتشار المسيحية، ثم انتشرت العربية بعد الفتح الإسلامي. ورغم ذلك ظلت الشخصية المصرية واحدة." },
              { who: "student", text: "ومتى ظهرت الحكومة المركزية؟" },
              { who: "teacher", text: "كان قيام حكومة مركزية قوية بعد توحيد القطرين على يد الملك مينا (نعرمر) هو الأساس الحقيقي لظهور الدولة المصرية؛ فتحولت من تجمعات صغيرة إلى دولة منظمة ذات سلطة واحدة ونظام إداري موحد." },
              { who: "student", text: "وما دور الجيش في ذلك؟" },
              { who: "teacher", text: "اعتمدت مصر على جيش من أبنائها يدافع عن أرضها؛ فهو الأداة التي تحمي حدود الدولة، وتصون إنجازاتها الحضارية، وتضمن استقرارها السياسي. وعقيدته: يبني ويحمي ويحافظ على الأرض ولا يعتدي." },
              { who: "student", text: "بقي آخر مقوم: الهوية." },
              { who: "teacher", text: "الهوية الوطنية مجموعة من الخصائص التي تميز الشعب المصري عبر التاريخ وتشكّل شخصيته وتعكس انتماءه الوطني، ومرتكزاتها: الدين، واللغة، والتاريخ." },
              { who: "student", text: "لقد فعلتها! فهمت الآن لماذا استمرت الدولة المصرية آلاف السنين." },
            ],
            example: [
              { title: "إجراءات الدولة المصرية للحفاظ على الهوية",
                table: { head: ["الإجراء", "مثال"], rows: [
                  ["حماية التراث الحضاري والاحتفال بالمناسبات الوطنية", "موكب المومياوات الملكية"],
                  ["تعريف الأجيال المختلفة بتاريخ مصر وحضارتها", "الأهرامات وأبو الهول"],
                  ["دعم الثقافة والفنون ودعم المؤسسات الثقافية", "دار الأوبرا المصرية وقصور الثقافة"],
                ] } },
              { title: "الجيش المصري",
                table: { head: ["الجيش هو الأداة التي", "عقيدته"], rows: [
                  ["تحمي حدود الدولة – تصون إنجازاتها الحضارية – تضمن استقرارها السياسي", "يبني ويحمي ويحافظ على الأرض ولا يعتدي"],
                ] } },
            ],
            task: {
              steps: [
                "اكتب مرتكزات الهوية الوطنية الثلاثة.",
                "اكتب إجراءً واحدًا تتخذه الدولة المصرية للحفاظ على الهوية.",
                "اضغط «تحقّق من إجابتك».",
              ],
              lang: "text",
              placeholder: "مرتكزات الهوية الوطنية: ...",
              hints: ["المرتكزات الثلاثة: الدين، و… ، و…", "تذكّر جدول إجراءات الدولة: التراث، وتعريف الأجيال، ودعم الثقافة."],
              check: ["الدين", "اللغة", "التاريخ"],
              model: "مرتكزات الهوية الوطنية: الدين – اللغة – التاريخ.\nمن إجراءات الدولة: حماية التراث الحضاري والاحتفال بالمناسبات الوطنية، وتعريف الأجيال بتاريخ مصر وحضارتها، ودعم الثقافة والفنون والمؤسسات الثقافية مثل دار الأوبرا المصرية وقصور الثقافة.",
            },
            checkpoint: [
              { type: "mcq", q: "«انصهر سكان وادي النيل في بوتقة ثقافية واحدة، مما جعل من الوحدة السياسية أمرًا طبيعيًا». العامل الأساسي الذي جعل الوحدة السياسية في مصر أمرًا طبيعيًا هو ..........", options: ["التجانس الثقافي والاجتماعي بين أبناء الشعب", "وفرة الموارد الاقتصادية والإنتاج الزراعي", "التأثر بالثقافات المجاورة الوافدة من الخارج", "قوة الجيش وفرض السيطرة بالعنف"], answer: 0, explain: "" },
              { type: "tf", q: "قامت الحكومة المركزية القوية في مصر بعد توحيد القطرين على يد الملك مينا (نعرمر).", answer: true, explain: "" },
              { type: "mcq", q: "تتمثل مرتكزات الهوية الوطنية في ..........", options: ["الدين – اللغة – التاريخ", "الجيش – الحدود – النيل", "الزراعة – الصناعة – التجارة", "الشعب – السلطة – السيادة"], answer: 0, explain: "" },
            ],
          },
        ],
        keypoints: [
          "الدولة وحدة سياسية تقوم على أربعة أركان مترابطة: الشعب، والحيز الجغرافي، والسلطة، والسيادة، وغياب أي ركن يهدد وجود الدولة ذاتها.",
          "توجد علاقة ارتباطية بين وعي الشعب بحقوقه وواجباته وبين قوة الدولة وتقدمها.",
          "أثبتت ثورة ١٩١٩م أن القوة الحقيقية في تحرير الوطن هي الشعب، وأن الوحدة الوطنية هي الأساس الأول للنجاح في التغيير.",
          "يشمل الحيز الجغرافي: اليابس، والمياه الإقليمية (١٢ ميلًا بحريًا من خط الأساس للساحل)، والمجال الجوي.",
          "تتكامل السلطات الثلاث: التشريعية (عين الشعب)، والتنفيذية (يد الدولة)، والقضائية (ميزان العدل).",
          "السيادة استقلال الدولة في قراراتها الداخلية والخارجية من دون خضوع لسلطة خارجية.",
          "مقومات نشأة الدولة المصرية: الموقع الجغرافي والحدود الطبيعية، والموارد الطبيعية، والتجانس البشري، والحكومة المركزية القوية، والقوة العسكرية، والحفاظ على الهوية.",
          "مصر «دولة نهر» بوجود النيل، و«دولة برزخ» بموقعها بين قارات العالم القديم.",
          "عقيدة الجيش المصري: يبني ويحمي ويحافظ على الأرض ولا يعتدي.",
        ],
        // كلمة الختام (بتظهر في آخر الدرس)
        closing: {
          title: "كلمة أخيرة",
          text: [
            "لقد فعلتها! أنهيت الدرس الأول بنجاح.",
            "الدولة المصرية لم تُبنَ في يوم وليلة؛ بل صنعها شعبٌ ارتبط بأرضه، ونيلٌ وهب الحياة، وجيشٌ يبني ويحمي ولا يعتدي، وهويةٌ راسخة استمرت عبر التاريخ. واليوم جاء دورك أنت: أن تعرف تاريخ وطنك لتحافظ على حاضره، وتشارك في بناء مستقبله.",
            "صُممت هذه المنصة بيد طالب مصري، إيمانًا بأن التعلّم الرقمي هو طريقنا إلى «مصر الرقمية».",
          ],
          motto: "من عرف تاريخه.. عرف طريقه",
        },
        // ملف للتحميل في آخر الدرس (محتاج إنترنت)
        download: {
          title: "كتاب «التاريخ الوطني» — الجزء الأول",
          desc: "كتاب الوزارة للصف الثاني الثانوي (PDF — حوالي ٥٧ ميجا). حمّله وارجع له في أي وقت.",
          file: "https://drive.google.com/uc?export=download&id=1yXADl_CWSlUNomP6HLGE2XGtgJ_26B1z",
          view: "https://drive.google.com/file/d/1yXADl_CWSlUNomP6HLGE2XGtgJ_26B1z/view?usp=sharing",
        },
      },
    ],
  },
    ],
  },
  /* ================= اللغة العربية (لسه مفيش محتوى) ================= */
  {
    id: "arabic", title: "اللغة العربية", icon: "bookPen", desc: "هيتضاف محتواها قريبًا",
    units: [],
  },
];


/* =====================================================================
   الجزء ٢: بنك الأسئلة (التدريب النهائي لكل درس)
   أسئلة الدرس الأول (التاريخ)
   type : "mcq" | "tf" | "fill" | "match" | "essay" (مقالي)      level: 1 سهل | 2 متوسط | 3 صعب
   ===================================================================== */
const QUESTIONS = [
  /* ---------- الدرس الأول: بناء الدولة المصرية واستمرارها عبر التاريخ ---------- */
  { id: "h1", lesson: "u1l1", type: "mcq", level: 1, q: "وحدة سياسية تشغل حيزًا جغرافيًا محددًا، وتضم شعبًا يقيم إقامة دائمة ويخضع لقوانينها، وتديرها سلطة منظمة، وتمارس سيادتها على شئونها الداخلية والخارجية. هذا تعريف ..........", options: ["الدولة", "الحكومة", "السلطة التنفيذية", "الحيز الجغرافي"], answer: 0, explain: "" },
  { id: "h2", lesson: "u1l1", type: "tf", level: 1, q: "وفقًا للمادة الرابعة من الدستور المصري: السيادة للشعب وحده، يمارسها ويحميها، وهو مصدر السلطات.", answer: true, explain: "" },
  { id: "h3", lesson: "u1l1", type: "fill", level: 1, q: "تمتد المياه الإقليمية عادة لمسافة ____ ميلًا بحريًا من خط الأساس للساحل.", answer: ["١٢"], options: ["١٢", "٢٢", "٢٥"], explain: "" },
  { id: "h4", lesson: "u1l1", type: "match", level: 2, q: "وصّل كل سلطة باختصاصها:", pairs: [
    { a: "السلطة التشريعية", b: "سن القوانين والرقابة على أعمال السلطة التنفيذية" },
    { a: "السلطة التنفيذية", b: "إدارة شئون الدولة" },
    { a: "السلطة القضائية", b: "الفصل في المنازعات وإصدار الأحكام وفقًا للقانون" }] },
  { id: "h5", lesson: "u1l1", type: "match", level: 2, q: "وصّل كل حدود من حدود مصر بموقعها:", pairs: [
    { a: "الحدود الشمالية", b: "خط ساحل البحر المتوسط" },
    { a: "الحدود الجنوبية", b: "دائرة عرض ٢٢ درجة شمالًا" },
    { a: "الحدود الغربية", b: "خط طول ٢٥ درجة شرقًا" },
    { a: "الحدود الشرقية", b: "ساحل البحر الأحمر وخليج العقبة والحدود مع فلسطين" }] },
  { id: "h6", lesson: "u1l1", type: "mcq", level: 2, q: "منع طائرة أجنبية غير مصرح لها بالطيران فوق السماء المصرية، وحماية حقول الغاز في المياه الإقليمية المصرية في البحر المتوسط، إجراءان يرتبطان بركن ..........", options: ["الحيز الجغرافي للدولة", "النفوذ الاقتصادي الإقليمي", "الحدود الطبيعية الواقية", "التواجد السكاني المتجانس"], answer: 0, explain: "الحيز الجغرافي يشمل اليابس والمياه الإقليمية والمجال الجوي." },
  { id: "h7", lesson: "u1l1", type: "mcq", level: 3, q: "«أثبتت ثورة ١٩١٩م أن الشعب المصري شعب مرتبط بأرضه.. حيث شارك الشعب بجميع فئاته.. ولأول مرة خرجت النساء في مظاهرات، وتضامن جميع المصريين تحت شعار (الدين لله والوطن للجميع)». يكمن الأثر السياسي المباشر لهذه المشاركة في ..........", options: ["ترسيخ مبدأ المواطنة وتعزيز الوعي الوطني", "تغيير طبيعة ونوع النظام الاقتصادي والإنتاجي للدولة", "التنازل عن ركن السلطة لصالح اللجان الشعبية المؤقتة", "الاقتصار على حماية الحدود التابعة للحيز الجغرافي"], answer: 0, explain: "" },
  { id: "h8", lesson: "u1l1", type: "mcq", level: 3, q: "«قام أهالي إحدى المناطق الساحلية بتشكيل لجان شعبية لحماية وتنظيم شئونهم في فترة غياب تام للأجهزة الأمنية». يتضح أن هذا المجتمع يمتلك ..........", options: ["ركني الشعب والحيز الجغرافي، بينما يفتقد لركن السلطة المكتملة", "ركني السلطة والسيادة، بينما يفتقد لركن الحيز الجغرافي", "ركن السيادة الخارجية فقط، بينما يفتقد لركني الشعب والأرض", "كافة أركان الدولة الأربعة بشكل تام ومكتمل الشروط"], answer: 0, explain: "غياب الأجهزة الأمنية يدل على غياب السلطة الحاكمة المكتملة." },
  { id: "h9", lesson: "u1l1", type: "mcq", level: 3, q: "يمكن تفسير الارتباط الوثيق بين مبدأ «السيادة الوطنية» وتحقيق التنمية الشاملة داخل الدولة بأن ..........", options: ["السيادة تضمن استقلالية إدارة الموارد", "التنمية تعوض نقص الحيز الجغرافي", "التنمية تقتصر على المقوم البشري", "السيادة تلغي دور السلطة التشريعية"], answer: 0, explain: "" },
  { id: "h10", lesson: "u1l1", type: "tf", level: 2, q: "«لم يكن للشعب دور في تحقيق التنمية الاقتصادية للدولة».", answer: false, explain: "يسهم الشعب في التنمية الاقتصادية بالعمل والإنتاج في مختلف القطاعات، ودفع الضرائب التي تموّل المشروعات العامة." },
  { id: "h11", lesson: "u1l1", type: "tf", level: 2, q: "«تحتكر الحكومة في الدولة المصرية سلطة سن القوانين والتشريعات».", answer: false, explain: "مجلس النواب (السلطة التشريعية) هو المختص بسن القوانين وفقًا للمادة (١٠١)، والحكومة هيئة تنفيذية وفقًا للمادة (١٦٣)." },
  { id: "h12", lesson: "u1l1", type: "fill", level: 1, q: "كان قيام حكومة مركزية قوية بعد توحيد القطرين على يد الملك ____ هو الأساس الحقيقي لظهور الدولة المصرية.", answer: ["مينا (نعرمر)"], options: ["مينا (نعرمر)", "سعد زغلول", "جمال حمدان"], explain: "" },
  { id: "h13", lesson: "u1l1", type: "mcq", level: 2, q: "رُسمت الحدود السياسية الفاصلة بين مصر وليبيا عام ..........", options: ["١٩٢٥م", "١٨٩٩م", "١٩٠٦م", "١٩١٩م"], answer: 0, explain: "١٨٩٩م: الحدود الجنوبية، و١٩٠٦م: الحدود البرية مع فلسطين." },
  { id: "h14", lesson: "u1l1", type: "mcq", level: 2, q: "«انصهر سكان وادي النيل في بوتقة ثقافية واحدة، مما جعل من الوحدة السياسية أمرًا طبيعيًا». العامل الأساسي في ذلك هو ..........", options: ["التجانس الثقافي والاجتماعي بين أبناء الشعب", "وفرة الموارد الاقتصادية والإنتاج الزراعي", "التأثر بالثقافات المجاورة الوافدة من الخارج", "قوة الجيش وفرض السيطرة بالعنف"], answer: 0, explain: "" },
  { id: "h15", lesson: "u1l1", type: "fill", level: 1, q: "مرتكزات الهوية الوطنية: الدين – اللغة – ____.", answer: ["التاريخ"], options: ["التاريخ", "الجيش", "الاقتصاد"], explain: "" },
  { id: "h16", lesson: "u1l1", type: "match", level: 3, q: "وصّل كل مصطلح بمعناه:", pairs: [
    { a: "السيادة", b: "استقلال الدولة في اتخاذ قراراتها من دون خضوع لسلطة خارجية" },
    { a: "المجال الجوي", b: "المساحة الرأسية التي تعلو اليابس والمياه الإقليمية" },
    { a: "دولة البرزخ", b: "دولة تربط بين كتلتين يابستين كبيرتين أو تفصل بين بحرين مهمين" },
    { a: "الهوية الوطنية", b: "خصائص تميز الشعب عبر التاريخ وتعكس انتماءه الوطني" }] },
  /* ---------- أسئلة مقالية: بيصححها «المصحح الذكي» (ملف ai_grader.py) ----------
     model  : الإجابة النموذجية من المنهج
     points : الأفكار الأساسية اللي لازم تكون في إجابة الطالب
     marks  : درجة السؤال */
  { id: "e1", lesson: "u1l1", type: "essay", level: 3, marks: 4, q: "«تنشأ الدولة نتيجة تضافر عدد من المقومات التي تحول المجتمع من تجمعات بشرية مشتتة إلى كيان سياسي منظم». وضّح مقومات نشأة الدولة المصرية.",
    model: "تنشأ الدولة نتيجة تضافر عدد من المقومات التي تحول المجتمع من تجمعات بشرية مشتتة إلى كيان سياسي منظم، وتتمثل مقومات نشأة الدولة المصرية في: الموقع الجغرافي والحدود الطبيعية، والموارد الطبيعية، والتجانس البشري والوحدة الوطنية، والحكومة المركزية القوية، والقوة العسكرية، والحفاظ على الهوية المصرية.",
    points: ["الموقع الجغرافي والحدود الطبيعية", "الموارد الطبيعية", "التجانس البشري والوحدة الوطنية", "الحكومة المركزية القوية", "القوة العسكرية", "الحفاظ على الهوية المصرية"] },
  { id: "e2", lesson: "u1l1", type: "essay", level: 3, marks: 4, q: "وضّح أثر خروج الشعب المصري في ثورة ١٩١٩م على أوضاع مصر السياسية.",
    model: "أظهر التعبير الصريح عن رفض الشعب للاحتلال البريطاني والتفاف المصريين حول هدف الاستقلال. وأجبرت المشاركة الشعبية سلطات الاحتلال على إعادة النظر في سياساتها. وأسهمت الثورة في تعزيز الوعي الوطني وترسيخ قيم المواطنة.",
    points: ["رفض الاحتلال البريطاني", "التفاف المصريين حول هدف الاستقلال", "إجبار الاحتلال على إعادة النظر في سياساته", "تعزيز الوعي الوطني وترسيخ قيم المواطنة"] },
  { id: "e3", lesson: "u1l1", type: "essay", level: 3, marks: 4, q: "وضّح: كيف ساعد الموقع الجغرافي لمصر على استقرارها عبر التاريخ؟",
    model: "ساعد الموقع الجغرافي المتميز لمصر على تحقيق استقرارها عبر التاريخ؛ حيث تمتعت بحدود طبيعية مثّلت حواجز قامت بدور حماية قلب الدولة (الوادي والدلتا)، وهو ما أتاح للمصريين الفرصة للتركيز على الزراعة والعمران وتنظيم شئون حياتهم، مما دعم نشأة الدولة وترسيخ دعائمها واستقرارها عبر العصور.",
    points: ["حدود طبيعية تمثل حواجز", "حماية الوادي والدلتا", "التركيز على الزراعة والعمران", "استقرار الدولة"] },
  { id: "e4", lesson: "u1l1", type: "essay", level: 3, marks: 4, q: "«الشعب المصري تميز بتجانس ثقافي فريد منذ البداية». في ضوء العبارة، استنتج أهمية تجانس الشعب المصري بالنسبة لقوة الدولة.",
    model: "تشكيل اتحاد متماسك يصعب تفتيته نتيجة انصهار السكان في بوتقة ثقافية واحدة منذ فجر التاريخ. وجعل الوحدة السياسية أمرًا طبيعيًا للدولة. وحماية المصريين من التأثر بثقافة المحتل، والحفاظ على الهوية المصرية المميزة واستقرار الدولة عبر التاريخ.",
    points: ["اتحاد متماسك يصعب تفتيته", "انصهار السكان في بوتقة ثقافية واحدة", "الوحدة السياسية أمر طبيعي", "عدم التأثر بثقافة المحتل والحفاظ على الهوية"] },
];



/* =====================================================================
   الجزء ٣: برمجة المنصة
   ===================================================================== */
(function () {
  "use strict";

  const $main = document.getElementById("app");
  const $top = document.getElementById("topbar");
  const history = [];
  let current = null;
  let timers = [];

  /* ---------------- أيقونات SVG ---------------- */
  const ICONS = {
    home: '<path d="M3 11l9-8 9 8"/><path d="M5 10v10h14V10"/><path d="M10 20v-6h4v6"/>',
    image: '<rect x="3" y="4" width="18" height="16" rx="2"/><circle cx="9" cy="10" r="2"/><path d="M21 17l-5-5-9 8"/>',
    pyramid: '<path d="M12 3L2 20h20L12 3z"/><path d="M12 3l3.5 17"/><path d="M7.2 11.5h7"/><path d="M4.6 16h11.3"/>',
    bookPen: '<path d="M3 5.5c2.5-1.2 5.5-1.2 8 .5v13c-2.5-1.7-5.5-1.7-8-.5z"/><path d="M11 6v13"/><path d="M14 19l.6-2.6 6-6a1.4 1.4 0 0 1 2 2l-6 6z"/><path d="M11 6c1.4-1 3-1.3 4.6-1.2"/>',
    login: '<path d="M14 4h4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2h-4"/><path d="M4 12h11"/><path d="M11 8l4 4-4 4"/>',
    userPlus: '<circle cx="10" cy="8" r="4"/><path d="M3 20a7 7 0 0 1 14 0"/><path d="M19 8v6M16 11h6"/>',
    back: '<path d="M5 12h14"/><path d="M13 6l6 6-6 6"/>',
    next: '<path d="M19 12H5"/><path d="M11 6l-6 6 6 6"/>',
    moon: '<path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8z"/>',
    sun: '<circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"/>',
    volume: '<path d="M11 5L6 9H2v6h4l5 4V5z"/><path d="M15.5 8.5a5 5 0 0 1 0 7M19 5a10 10 0 0 1 0 14"/>',
    mute: '<path d="M11 5L6 9H2v6h4l5 4V5z"/><path d="M22 9l-6 6M16 9l6 6"/>',
    power: '<path d="M12 2v10"/><path d="M18.4 6.6a9 9 0 1 1-12.8 0"/>',
    book: '<path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20V3H6.5A2.5 2.5 0 0 0 4 5.5v14z"/><path d="M4 19.5A2.5 2.5 0 0 0 6.5 22H20v-5"/>',
    layers: '<path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/>',
    target: '<circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="5"/><circle cx="12" cy="12" r="1"/>',
    chat: '<path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/><path d="M8 9h8M8 13h5"/>',
    bulb: '<path d="M9 18h6M10 22h4"/><path d="M12 2a7 7 0 0 0-4 12.7V17h8v-2.3A7 7 0 0 0 12 2z"/>',
    list: '<path d="M8 6h13M8 12h13M8 18h13"/><circle cx="4" cy="6" r="1"/><circle cx="4" cy="12" r="1"/><circle cx="4" cy="18" r="1"/>',
    pencil: '<path d="M12 20h9"/><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4z"/>',
    gear: '<circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.8l.1.1a2 2 0 1 1-2.8 2.8l-.1-.1a1.7 1.7 0 0 0-1.8-.3 1.7 1.7 0 0 0-1 1.5V21a2 2 0 1 1-4 0v-.1a1.7 1.7 0 0 0-1.1-1.5 1.7 1.7 0 0 0-1.8.3l-.1.1a2 2 0 1 1-2.8-2.8l.1-.1a1.7 1.7 0 0 0 .3-1.8 1.7 1.7 0 0 0-1.5-1H3a2 2 0 1 1 0-4h.1a1.7 1.7 0 0 0 1.5-1.1 1.7 1.7 0 0 0-.3-1.8l-.1-.1a2 2 0 1 1 2.8-2.8l.1.1a1.7 1.7 0 0 0 1.8.3H9a1.7 1.7 0 0 0 1-1.5V3a2 2 0 1 1 4 0v.1a1.7 1.7 0 0 0 1 1.5 1.7 1.7 0 0 0 1.8-.3l.1-.1a2 2 0 1 1 2.8 2.8l-.1.1a1.7 1.7 0 0 0-.3 1.8V9a1.7 1.7 0 0 0 1.5 1H21a2 2 0 1 1 0 4h-.1a1.7 1.7 0 0 0-1.5 1z"/>',
    notebook: '<rect x="5" y="3" width="14" height="18" rx="2"/><path d="M9 7h6M9 11h6M9 15h4"/>',
    medal: '<circle cx="12" cy="15" r="6"/><path d="M8.5 10.5L6 2h4l2 5 2-5h4l-2.5 8.5"/>',
    timer: '<circle cx="12" cy="13" r="8"/><path d="M12 9v4l2 2M9 2h6"/>',
    grid: '<rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/>',
    cards: '<rect x="2" y="6" width="13" height="16" rx="2"/><path d="M7 2h13a2 2 0 0 1 2 2v13"/>',
    help: '<circle cx="12" cy="12" r="9"/><path d="M9.1 9a3 3 0 0 1 5.8 1c0 2-3 3-3 3"/><path d="M12 17h.01"/>',
    user: '<circle cx="12" cy="8" r="4"/><path d="M4 21a8 8 0 0 1 16 0"/>',
    board: '<rect x="3" y="3" width="18" height="12" rx="1.5"/><path d="M12 15v4M8 21l4-2 4 2"/>',
    search: '<circle cx="11" cy="11" r="7"/><path d="M21 21l-4.3-4.3"/>',
    check: '<path d="M20 6L9 17l-5-5"/>',
    x: '<path d="M18 6L6 18M6 6l12 12"/>',
    trophy: '<path d="M8 21h8M12 17v4M7 4h10v5a5 5 0 0 1-10 0V4z"/><path d="M17 5h3a3 3 0 0 1-3 4M7 5H4a3 3 0 0 0 3 4"/>',
    star: '<path d="M12 2l3.1 6.3 6.9 1-5 4.9 1.2 6.8L12 17.8 5.8 21l1.2-6.8-5-4.9 6.9-1z"/>',
    flame: '<path d="M12 22c4 0 7-3 7-7 0-4-3-6-4-10-2 2-3 4-3 6-1-1-2-2-2-4-2 2-5 5-5 8 0 4 3 7 7 7z"/>',
    sprout: '<path d="M12 22V11"/><path d="M12 11C12 7 9 4 4 4c0 5 3 7 8 7z"/><path d="M12 13c0-3 3-6 8-6 0 4-3 6-8 6z"/>',
    cap: '<path d="M22 10L12 5 2 10l10 5 10-5z"/><path d="M6 12v5c3 2 9 2 12 0v-5"/>',
    file: '<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6M8 13h8M8 17h5"/>',
    sparkle: '<path d="M12 3l1.9 5.1L19 10l-5.1 1.9L12 17l-1.9-5.1L5 10l5.1-1.9z"/><path d="M19 17l.7 1.8 1.8.7-1.8.7L19 22l-.7-1.8-1.8-.7 1.8-.7z"/>',
    refresh: '<path d="M21 12a9 9 0 1 1-3-6.7L21 8"/><path d="M21 3v5h-5"/>',
    printer: '<path d="M6 9V2h12v7"/><rect x="3" y="9" width="18" height="8" rx="2"/><path d="M6 14h12v8H6z"/>',
    trash: '<path d="M3 6h18M8 6V4h8v2M19 6l-1 14H6L5 6"/>',
    plus: '<path d="M12 5v14M5 12h14"/>',
    download: '<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5M12 15V3"/>',
    upload: '<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8l-5-5-5 5M12 3v12"/>',
    code: '<path d="M16 18l6-6-6-6M8 6l-6 6 6 6"/>',
    eye: '<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8S1 12 1 12z"/><circle cx="12" cy="12" r="3"/>',
    save: '<path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><path d="M17 21v-8H7v8M7 3v5h8"/>',
    chart: '<path d="M3 3v18h18"/><path d="M7 16v-5M12 16V8M17 16v-8"/>',
    play: '<path d="M7 4l13 8-13 8z"/>',
    lock: '<rect x="4" y="11" width="16" height="10" rx="2"/><path d="M8 11V7a4 4 0 0 1 8 0v4"/>',
    copy: '<rect x="9" y="9" width="12" height="12" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>',
    info: '<circle cx="12" cy="12" r="9"/><path d="M12 16v-4M12 8h.01"/>',
    alert: '<path d="M10.3 3.9L1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z"/><path d="M12 9v4M12 17h.01"/>',
    logout: '<path d="M15 21h4a2 2 0 0 0 2-2V5a2 2 0 0 0-2-2h-4"/><path d="M8 17l-5-5 5-5M3 12h12"/>',
    trend: '<path d="M23 6l-9.5 9.5-5-5L1 18"/><path d="M17 6h6v6"/>',
    flag: '<path d="M4 22V4M4 4h13l-2 4 2 4H4"/>',
    shield: '<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M9 12l2 2 4-4"/>',
    swap: '<path d="M17 3l4 4-4 4M21 7H9M7 21l-4-4 4-4M3 17h12"/>',
    link: '<path d="M10 13a5 5 0 0 0 7.5.5l3-3a5 5 0 0 0-7-7l-1.7 1.7"/><path d="M14 11a5 5 0 0 0-7.5-.5l-3 3a5 5 0 0 0 7 7l1.7-1.7"/>',
    skip: '<path d="M19 20L9 12l10-8z"/><path d="M5 19V5"/>',
    send: '<path d="M22 2L11 13"/><path d="M22 2l-7 20-4-9-9-4z"/>',
    brain: '<path d="M9 4a3 3 0 0 0-3 3 3 3 0 0 0-2 5 3 3 0 0 0 2 5 3 3 0 0 0 3 3h1V4z"/><path d="M15 4a3 3 0 0 1 3 3 3 3 0 0 1 2 5 3 3 0 0 1-2 5 3 3 0 0 1-3 3h-1V4z"/><path d="M6 12h4M14 12h4M7 8h3M14 16h3"/>',
    cup: '<path d="M17 8h1a4 4 0 0 1 0 8h-1"/><path d="M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4z"/><path d="M6 2v2M10 2v2M14 2v2"/>',
  };
  const I = (n, extra = "") => `<svg class="i" viewBox="0 0 24 24" aria-hidden="true" ${extra}>${ICONS[n] || ""}</svg>`;

  /* شخصيات الحوار (SVG) */
  const AVATARS = {
    teacher: `<svg class="avatar" viewBox="0 0 64 64" aria-hidden="true">
      <circle cx="32" cy="32" r="32" fill="#ead8c2"/>
      <path d="M11 62c2-13 10-19 21-19s19 6 21 19z" fill="#8a6245"/>
      <path d="M26 43l6 8 6-8z" fill="#fffaf3"/><path d="M31 51l1 7 1-7z" fill="#5b3f2c"/>
      <circle cx="32" cy="28" r="12.5" fill="#f1cfae"/>
      <path d="M19.5 27c-.5-10 5.5-15 12.5-15s13 5 12.5 15c-2.5-4.5-7-7-12.5-7s-10 2.5-12.5 7z" fill="#5b3f2c"/>
      <circle cx="27" cy="29" r="3.4" fill="none" stroke="#5b3f2c" stroke-width="1.6"/>
      <circle cx="37" cy="29" r="3.4" fill="none" stroke="#5b3f2c" stroke-width="1.6"/>
      <path d="M30.4 29h3.2" stroke="#5b3f2c" stroke-width="1.6"/>
      <circle cx="27" cy="29" r="1.1" fill="#3e2c20"/><circle cx="37" cy="29" r="1.1" fill="#3e2c20"/>
      <path d="M28 35q4 3 8 0" stroke="#8a4b33" stroke-width="1.7" fill="none" stroke-linecap="round"/>
    </svg>`,
    student: `<svg class="avatar" viewBox="0 0 64 64" aria-hidden="true">
      <circle cx="32" cy="32" r="32" fill="#f3e6d3"/>
      <path d="M11 62c2-13 10-19 21-19s19 6 21 19z" fill="#b88a62"/>
      <path d="M25 44q7 6 14 0" fill="none" stroke="#fffaf3" stroke-width="2.5"/>
      <circle cx="32" cy="29" r="12.5" fill="#f3d3b3"/>
      <path d="M19.5 28c-1-10.5 5-16 12.5-16 8 0 13.5 5.5 12.5 15-2-3.5-5-5.5-8.5-5.5-2.5 2.5-6.5 3.2-10.5 2.2-2.8 1-4.8 2.3-6 4.3z" fill="#3e2c20"/>
      <circle cx="27" cy="30" r="1.7" fill="#3e2c20"/><circle cx="37" cy="30" r="1.7" fill="#3e2c20"/>
      <circle cx="24" cy="34" r="2" fill="#eab79a" opacity=".6"/><circle cx="40" cy="34" r="2" fill="#eab79a" opacity=".6"/>
      <path d="M28.5 35.5q3.5 3 7 0" stroke="#8a4b33" stroke-width="1.7" fill="none" stroke-linecap="round"/>
    </svg>`,
  };

  const cupSVG = (cls) => `
    <svg class="${cls}" viewBox="0 0 120 120" aria-hidden="true">
      <g class="steam" fill="none" stroke="#b88a62" stroke-width="4" stroke-linecap="round">
        <path d="M45 30 q-6 -8 0 -16 q6 -8 0 -14"/><path d="M60 30 q-6 -8 0 -16 q6 -8 0 -14"/><path d="M75 30 q-6 -8 0 -16 q6 -8 0 -14"/>
      </g>
      <path d="M22 42 h72 v30 a30 30 0 0 1 -30 30 h-12 a30 30 0 0 1 -30 -30z" fill="#8a6245"/>
      <path d="M26 46 h64 v8 h-64z" fill="#d9c2a7"/>
      <path d="M94 50 h6 a13 13 0 0 1 0 26 h-8" fill="none" stroke="#8a6245" stroke-width="7"/>
      <ellipse cx="58" cy="106" rx="44" ry="6" fill="#d9c2a7"/>
      <path d="M58 68l3 7 7 3-7 3-3 7-3-7-7-3 7-3z" fill="#fffaf3"/>
    </svg>`;

  /* ---------------- أدوات مساعدة ---------------- */
  const shuffle = (a) => { a = a.slice(); for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]]; } return a; };
  const ar = (n) => String(n).replace(/\d/g, (d) => "٠١٢٣٤٥٦٧٨٩"[d]);
  const esc = (s) => String(s ?? "").replace(/[&<>"]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" }[c]));
  const norm = (s) => String(s ?? "").replace(/[\u064B-\u065F\u0670\u0640]/g, "").replace(/[أإآٱ]/g, "ا").replace(/ة/g, "ه").replace(/ى/g, "ي").replace(/\s+/g, " ").trim().toLowerCase();
  const clearTimers = () => { timers.forEach(clearInterval); timers = []; };
  const $ = (sel, root = $main) => root.querySelector(sel);
  const $$ = (sel, root = $main) => Array.from(root.querySelectorAll(sel));
  const pct = (a, b) => (b ? Math.round((a / b) * 100) : 0);
  const PRAISE = ["رائع!", "خطوة ممتازة!", "لقد فعلتها!", "أحسنت!", "برافو عليك!", "ممتاز، كمّل كده!"];
  const praise = () => PRAISE[Math.floor(Math.random() * PRAISE.length)];

  // الحفظ في المتصفح: كل حساب ليه تقدّمه لوحده، والحسابات والوضع الليلي مشتركين
  const GLOBAL_KEYS = ["users", "user", "theme"];
  let session = null; // اسم المستخدم اللي داخل حاليًا
  const skey = (k) => "mp_" + (session && !GLOBAL_KEYS.includes(k) ? `u_${session}_` : "") + k;
  const store = {
    get(k, d) { try { const v = localStorage.getItem(skey(k)); return v ? JSON.parse(v) : d; } catch (e) { return d; } },
    set(k, v) { try { localStorage.setItem(skey(k), JSON.stringify(v)); } catch (e) {} },
    del(k) { try { localStorage.removeItem(skey(k)); } catch (e) {} },
  };
  session = store.get("user", null);

  function toast(msg) {
    const t = document.createElement("div");
    t.className = "toast"; t.textContent = msg;
    document.body.appendChild(t);
    setTimeout(() => t.remove(), 2400);
  }
  function modal(html, onReady) {
    const m = document.createElement("div");
    m.className = "modal-bg";
    m.innerHTML = `<div class="modal">${html}</div>`;
    document.body.appendChild(m);
    m.onclick = (e) => { if (e.target === m) m.remove(); };
    onReady && onReady(m, () => m.remove());
    return m;
  }
  function confirmBox(title, text, yesLabel, onYes) {
    modal(`<div class="big-ico bad">${I("alert")}</div><h3>${title}</h3><p style="color:var(--muted)">${text}</p>
      <div class="btn-row" style="justify-content:center"><button class="btn" data-y>${yesLabel}</button><button class="btn soft" data-n>إلغاء</button></div>`,
      (m, close) => { m.querySelector("[data-n]").onclick = close; m.querySelector("[data-y]").onclick = () => { close(); onYes(); }; });
  }

  /* ---------------- الأصوات ---------------- */
  let soundOn = store.get("sound", true);
  let actx = null;
  function tone(freqs, dur = 0.12, type = "sine", vol = 0.08) {
    if (!soundOn) return;
    try {
      actx = actx || new (window.AudioContext || window.webkitAudioContext)();
      freqs.forEach((f, i) => {
        const o = actx.createOscillator(), g = actx.createGain();
        o.type = type; o.frequency.value = f;
        const t0 = actx.currentTime + i * dur;
        g.gain.setValueAtTime(vol, t0);
        g.gain.exponentialRampToValueAtTime(0.0001, t0 + dur * 1.6);
        o.connect(g); g.connect(actx.destination);
        o.start(t0); o.stop(t0 + dur * 1.7);
      });
    } catch (e) {}
  }
  const sfx = {
    click: () => tone([520], 0.06, "sine", 0.05),
    pop: () => tone([740], 0.05, "sine", 0.04),
    ok: () => tone([660, 880], 0.1),
    bad: () => tone([300, 220], 0.12, "triangle", 0.07),
    win: () => tone([523, 659, 784, 1046], 0.13),
  };
  function speak(text) {
    if (!("speechSynthesis" in window)) return toast("المتصفح ده مش بيدعم القراءة الصوتية");
    speechSynthesis.cancel();
    const u = new SpeechSynthesisUtterance(text);
    u.lang = "ar-EG"; u.rate = 0.9;
    const v = speechSynthesis.getVoices().find((x) => x.lang && x.lang.startsWith("ar"));
    if (v) u.voice = v; else toast("مفيش صوت عربي متسطب على الجهاز ده");
    speechSynthesis.speak(u);
  }

  /* ---------------- البيانات ---------------- */
  const TYPES = { mcq: "اختيار من متعدد", tf: "صح أم خطأ", fill: "أكمل", match: "توصيل", essay: "سؤال مقالي" };
  const LEVELS = { 1: "سهل", 2: "متوسط", 3: "صعب" };
  // نسخة من المنهج زي ما هو (قبل الربط اللي تحت) — دي اللي المصحح الذكي بيتعلم منها
  const CURRICULUM_JSON = JSON.stringify(SUBJECTS);
  // كل درس بيعرف هو في أنهي قسم وأنهي وحدة
  const LESSONS = SUBJECTS.flatMap((s) => s.units.flatMap((u, ui) => { u.subject = s; u.unum = ui + 1;
    return u.lessons.map((l, i) => Object.assign(l, { subject: s, unit: u, num: i + 1, unum: ui + 1, parts: l.parts || [] })); }));
  const subjectById = (id) => SUBJECTS.find((s) => s.id === id);
  const subjectLessons = (s) => s.units.flatMap((u) => u.lessons);
  const lessonById = (id) => LESSONS.find((l) => l.id === id);
  const lessonName = (id) => { const l = lessonById(id); return l ? `${l.subject.title} › ${l.title}` : "درس غير معروف"; };
  const bank = () => QUESTIONS;
  const qOf = (lessonId) => bank().filter((q) => q.lesson === lessonId);
  const typeTag = (t) => `<span class="tag">${TYPES[t] || t}</span>`;
  const levelTag = (l) => `<span class="tag l${l}">${LEVELS[l] || ""}</span>`;

  /* ---------------- التقدم ---------------- */
  const progress = () => store.get("progress", {});
  function saveLesson(id, score, total) {
    const p = progress(), prev = p[id];
    if (!prev || score / total >= prev.score / prev.total) p[id] = { score, total };
    store.set("progress", p);
  }
  const doneCount = () => LESSONS.filter((l) => progress()[l.id]).length;
  const cps = () => store.get("cp", {});
  const cpPassed = (lid, i) => !!(cps()[lid] || {})[i];
  const setCp = (lid, i) => { const c = cps(); c[lid] = c[lid] || {}; c[lid][i] = true; store.set("cp", c); };
  const partsDone = (l) => l.parts.filter((_, i) => cpPassed(l.id, i)).length;
  const stats = () => store.get("stats", { answered: 0, correct: 0, streak: 0, bestStreak: 0, exams: 0, certs: 0 });
  function recordAnswer(ok) {
    const s = stats(); s.answered++;
    if (ok) { s.correct++; s.streak++; s.bestStreak = Math.max(s.bestStreak, s.streak); } else s.streak = 0;
    store.set("stats", s);
  }

  /* ---------------- الامتحان النهائي + المصحح الذكي ----------------
     المصحح الذكي برنامج Python (ملف ai_grader.py) بيشتغل على نفس الجهاز:
     المنصة بتبعتله الإجابات، وهو بيراجعها سؤال سؤال ويرجّع النتيجة */
  /* =====================================================================
     المصحح الذكي (ai_grader.py) — شغال جوه المتصفح نفسه بـ Pyodide (Python جوه المتصفح)
     - ملف ai_grader.py بيتحمّل من index.html، وبيشتغل في الخلفية (Web Worker) عشان الصفحة ما تهنّجش
     - أول مرة بيتعلم المنهج ويحفظ الشبكة في المتصفح، وبعد كده بيفتح على طول
     - لو مفيش نت يحمّل Pyodide، بيجرب السيرفر (python ai_grader.py) لو شغال
     ===================================================================== */
  const PYODIDE_URL = "https://cdn.jsdelivr.net/pyodide/v314.0.7/full/";
  // لو حبيت تشغّل المصحح على سيرفر (Render مثلًا) بدل المتصفح، اكتب لينكه هنا كبديل
  const GRADER_URL = "";
  const API = GRADER_URL ? GRADER_URL.replace(/\/$/, "") + "/api/" : location.protocol.startsWith("http") ? "api/" : "http://localhost:8765/api/";
  const LOCAL = location.protocol === "file:" || /^(localhost|127\.|0\.0\.0\.0|\[::1\])/.test(location.hostname);
  // Worker عادي (عشان يشتغل كمان لما index.html يتفتح كملف من الجهاز)، وPyodide بيتحمّل جواه بـ import()
  const WORKER_SRC = `
    let py = null;
    self.importScripts = function () { throw new Error("module-style worker"); };
    self.onmessage = async (e) => {
      const { id, cmd, args } = e.data;
      try {
        if (cmd === "init") {
          const { loadPyodide } = await import(args.url + "pyodide.mjs");
          py = await loadPyodide({ indexURL: args.url, stdout: (t) => self.postMessage({ log: t }) });
          py.globals.set("__browser_src__", args.full);
          py.runPython(args.code);
          self.postMessage({ id, ok: true, value: py.globals.get("browser_start")(args.data, args.model || "") });
        } else self.postMessage({ id, ok: true, value: py.globals.get(cmd)(...args) });
      } catch (err) { self.postMessage({ id, ok: false, error: String((err && err.message) || err) }); }
    };`;
  const AI = { worker: null, ready: null, seq: 0, wait: {}, last: "", running: {}, failedAt: 0 };
  const aiSend = (cmd, args) => new Promise((res, rej) => {
    const id = ++AI.seq; AI.wait[id] = { res, rej }; AI.worker.postMessage({ id, cmd, args });
  });
  function aiReady() {
    if (AI.ready) return AI.ready;
    AI.ready = (async () => {
      let full = window.AI_GRADER_SRC;
      if (!full) full = await (await fetch("ai_grader.py", { cache: "no-store" })).text();
      const a = full.indexOf("# ==PY-START=="), b = full.lastIndexOf("# ==PY-END==");
      if (a < 0 || b < a) throw new Error("ملف المصحح مش كامل");
      let model = "";
      try { model = localStorage.getItem("mp_ai_model") || ""; } catch (e) {}
      if (!model && location.protocol.startsWith("http")) {
        try { const r = await fetch("ai_model.json"); if (r.ok) model = await r.text(); } catch (e) {}
      }
      const w = new Worker(URL.createObjectURL(new Blob([WORKER_SRC], { type: "text/javascript" })));
      w.onmessage = (e) => {
        const m = e.data;
        if (m.log !== undefined) { AI.last = String(m.log).trim(); return; }
        const pr = AI.wait[m.id]; if (!pr) return;
        delete AI.wait[m.id]; m.ok ? pr.res(m.value) : pr.rej(new Error(m.error));
      };
      w.onerror = () => { Object.values(AI.wait).forEach((pr) => pr.rej(new Error("worker"))); AI.wait = {}; };
      AI.worker = w;
      const init = aiSend("init", { url: PYODIDE_URL, full, code: full.slice(a, b), model,
        data: `{"subjects":${CURRICULUM_JSON},"questions":${JSON.stringify(QUESTIONS)},"site":${JSON.stringify(SITE.name)}}` });
      const r = JSON.parse(await Promise.race([init, new Promise((_, rej) => setTimeout(() => rej(new Error("timeout")), 120000))]));
      if (r.trained && r.model) { try { localStorage.setItem("mp_ai_model", JSON.stringify(r.model)); } catch (e) {} }
      return true;
    })();
    AI.ready.catch((e) => { console.info("المصحح الذكي مااتحمّلش:", String((e && e.message) || e).slice(-300)); AI.ready = null; AI.failedAt = Date.now(); if (AI.worker) AI.worker.terminate(); AI.worker = null; });
    return AI.ready;
  }
  const aiWarm = () => { if (!AI.ready && Date.now() - AI.failedAt > 30000) aiReady().catch(() => {}); };

  // البديل: المصحح على سيرفر (python ai_grader.py على الجهاز، أو GRADER_URL)
  async function remoteApi(path, body) {
    const r = await fetch(API + path, body
      ? { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }
      : { cache: "no-store" });
    let j = {}; try { j = (await r.json()) || {}; } catch (e) {}
    if (!r.ok) {
      // رد من المصحح نفسه (فيه رسالة) = خطأ حقيقي. غير كده (صفحة خطأ أو السيرفر نايم) = المصحح مش متاح
      const err = new Error(j.error || "المصحح الذكي مش متاح دلوقتي");
      err.server = !!j.error; throw err;
    }
    if (!Object.keys(j).length) { const err = new Error("المصحح الذكي مش متاح دلوقتي"); err.server = false; throw err; }
    return j;
  }

  // مكتب المراجعة جوه المتصفح: نفس فكرة الطابور اللي في ai_grader.py
  const subs = () => store.get("aisubs", {});
  const setSub = (sid, patch) => {
    const all = subs(); if (!all[sid]) return;
    Object.assign(all[sid], patch);
    const ids = Object.keys(all).sort((x, y) => all[y].created - all[x].created);
    ids.slice(6).forEach((k) => delete all[k]); // بنحتفظ بآخر ٦ امتحانات بس
    store.set("aisubs", all);
  };
  const sleep = (ms) => new Promise((r) => setTimeout(r, ms));
  async function runReview(sid) {
    if (AI.running[sid]) return;
    AI.running[sid] = true;
    const owner = session;
    try {
      let s = subs()[sid];
      if (!s) return;
      setSub(sid, { status: "queued", phase: "loading" });
      let local = true;
      try { await aiReady(); } catch (e) { local = false; }
      if (session !== owner) return;
      if (!local) { // مفيش نت لـ Pyodide: نجرب السيرفر
        try {
          const r = await remoteApi("submit", { lesson: s.lesson, user: owner || "", answers: s.answers });
          setSub(sid, { remote: r.id, phase: "" }); return;
        } catch (e) { setSub(sid, { phase: "offline", retryAt: Date.now() + 15000 }); return; }
      }
      setSub(sid, { status: "reviewing", phase: "" });
      const items = []; let got = 0, total = 0;
      for (let n = 0; n < s.answers.length; n++) {
        const a = s.answers[n], q = QUESTIONS.find((x) => x.id === a.id);
        await sleep(q && q.type === "essay" ? 1400 : 350); // المصحح بياخد وقته في المراجعة
        const it = JSON.parse(await aiSend("browser_grade", [a.id, JSON.stringify(a.value)]));
        if (it.error) continue;
        items.push(it); got += it.score; total += it.marks;
        if (session !== owner) return;
        setSub(sid, { progress: { done: n + 1, total: s.answers.length, current: it.type } });
      }
      const percent = total ? Math.round((got / total) * 100) : 0;
      setSub(sid, { status: "done", result: { score: got, total, percent, passed: percent >= PASS, items,
        reviewed: Date.now() / 1000, reviewer: "المصحح الذكي 1.0 (Python جوه المتصفح)" } });
    } catch (e) {
      if (session === owner) setSub(sid, { status: "error", error: String(e.message || e) });
    } finally { delete AI.running[sid]; }
  }
  async function localApi(path, body) {
    if (path === "check") {
      try { await aiReady(); } catch (e) { return remoteApi(path, body); }
      return JSON.parse(await aiSend("browser_check", [body.lesson, body.part, String(body.text || "")]));
    }
    if (path === "submit") {
      const answers = (body.answers || []).filter((a) => QUESTIONS.some((q) => q.id === a.id && q.lesson === body.lesson));
      if (!answers.length) { const err = new Error("مفيش إجابات"); err.server = true; throw err; }
      const sid = "b" + Date.now().toString(36) + Math.random().toString(36).slice(2, 6);
      const all = subs();
      all[sid] = { id: sid, lesson: body.lesson, created: Date.now(), status: "queued", answers, progress: { done: 0, total: answers.length } };
      store.set("aisubs", all);
      runReview(sid);
      return { id: sid, status: "queued" };
    }
    if (path.startsWith("result/")) {
      const sid = path.slice(7), s = subs()[sid];
      if (!s) {
        if (!sid.startsWith("b")) return remoteApi(path); // امتحان قديم اتبعت للسيرفر
        const err = new Error("الامتحان ده مش موجود"); err.server = true; throw err;
      }
      if (s.remote) { // اتصحح على السيرفر
        const r = await remoteApi("result/" + s.remote);
        if (r.status === "done") setSub(sid, { status: "done", result: r.result });
        return r;
      }
      if ((s.status === "queued" || s.status === "reviewing") && !AI.running[sid] && (!s.retryAt || Date.now() >= s.retryAt)) runReview(sid);
      const out = { id: sid, status: s.status, progress: s.progress, result: s.result };
      if (s.status === "queued" && s.phase === "loading") {
        const m = /(\d+)\/(\d+)/.exec(AI.last || "");
        out.noteTitle = "المصحح الذكي بيجهز";
        out.note = m ? `بيتعلم المنهج (دورة ${ar(+m[1])} من ${ar(+m[2])}) — ده بيحصل أول مرة بس.` : "بيشغّل Python جوه المتصفح — أول مرة بتاخد شوية وقت.";
      } else if (s.status === "queued" && s.phase === "offline") {
        out.noteTitle = "مستني المصحح الذكي";
        out.note = "المصحح محتاج إنترنت أول مرة عشان يتحمّل. أول ما النت يرجع، التصحيح هيكمل لوحده.";
      }
      return out;
    }
    return remoteApi(path, body);
  }
  const api = (path, body) => localApi(path, body);
  const exams = () => store.get("exam", {});
  const examOf = (lid) => exams()[lid] || null;
  const setExam = (lid, v) => { const e = exams(); e[lid] = v; store.set("exam", e); };
  const examPending = (e) => !!(e && e.sid && (e.status === "queued" || e.status === "reviewing"));
  const MARKS = { mcq: 1, tf: 1, fill: 1, match: 2 }, PASS = 50;
  const marksOf = (q) => (q.type === "essay" ? q.marks || 4 : MARKS[q.type] || 1);
  const marksWord = (m) => (m === 1 ? "درجة واحدة" : m === 2 ? "درجتين" : `${ar(m)} درجات`);
  const qWord = (n) => (n === 1 ? "سؤال واحد" : n === 2 ? "سؤالين" : `${ar(n)} ${n <= 10 ? "أسئلة" : "سؤال"}`);
  const num = (x) => ar(String(Math.round(x * 10) / 10).replace(".", "٫"));

  /* ---------------- التنقل ---------------- */
  function go(name, params = {}, push = true) {
    clearTimers();
    if ("speechSynthesis" in window) speechSynthesis.cancel();
    if (push && current) history.push(current);
    current = { name, params };
    window.onbeforeunload = null;
    const full = name === "bye", locked = name === "exam";
    $top.classList.toggle("hidden", full || locked);
    document.body.classList.toggle("exam-mode", locked);
    $main.style.maxWidth = full ? "none" : "";
    $main.style.padding = full ? "0" : "";
    screens[name](params);
    window.scrollTo(0, 0);
    setNav(NAV_OF[name]);
  }

  /* الإطار المتحرك بتاع القائمة: بيروح عند الزرار اللي عليه الماوس، ولما يسيبه يرجع للصفحة الحالية */
  const $navBox = document.querySelector(".nav-box"), $rect = document.querySelector(".main-nav .rect");
  const NAV_OF = { home: "home", subject: "lessons", lesson: "lessons", examStatus: "lessons" };
  function outlineTo(btn) {
    if (!btn || !$navBox.clientWidth) { $rect.style.strokeDashoffset = "5"; $rect.style.strokeDasharray = "0 0 10 40 10 40"; return; }
    const w = $navBox.clientWidth, h = $navBox.clientHeight, P = 2 * (w + h);
    const x = btn.offsetLeft, bw = btn.offsetWidth;
    const s = (x / P) * 100, len = (bw / P) * 100, bottom = ((w + h + (w - x - bw)) / P) * 100;
    $rect.style.strokeDashoffset = "0";
    $rect.style.strokeDasharray = `0 ${s} ${len} ${bottom - s - len} ${len} ${100 - bottom - len}`;
  }
  function setNav(key) {
    $$(".nav-btn", $navBox).forEach((b) => b.classList.toggle("active", b.dataset.nav === key));
    outlineTo($(".nav-btn.active", $navBox));
  }
  const render = (html) => { $main.innerHTML = `<section class="screen">${html}</section>`; };
  const title = (ic, t) => `<h1 class="page-title" style="display:flex;align-items:center;gap:12px">${I(ic, 'style="width:32px;height:32px;color:var(--coffee)"')}${t}</h1>`;
  function crumbs(items) {
    return `<nav class="crumbs"><button data-crumb="home">${I("home")} الرئيسية</button>${items.map((it) => `<span class="sep">›</span>${it.go ? `<button data-crumb='${JSON.stringify(it.go)}'>${esc(it.t)}</button>` : `<span>${esc(it.t)}</span>`}`).join("")}</nav>`;
  }
  function bindCrumbs() {
    $$("[data-crumb]").forEach((b) => (b.onclick = () => {
      sfx.click();
      if (b.dataset.crumb === "home") return go("home");
      const g = JSON.parse(b.dataset.crumb); go(g[0], g[1] || {});
    }));
  }
  function bindGo() { $$("[data-go]").forEach((b) => (b.onclick = () => { sfx.click(); go(b.dataset.go); })); }
  const tile = (attr, ic, t, p, extra = "") => `<button class="tile" ${attr}><span class="ico">${I(ic)}</span><h3>${t}</h3><p>${p}</p>${extra}</button>`;

  /* =====================================================================
     عرض سؤال واحد (٤ أنواع) — done(صح؟, إجابة الطالب)
     ===================================================================== */
  const MATCH_COLORS = ["#8a6245", "#6f8f5f", "#b3624a", "#5d7a99", "#a07a3c", "#7d5a8a", "#4f8a86", "#9a5a6a"];

  function renderQ(q, box, done) {
    let answered = false;
    if (q.id) box.dataset.qid = q.id;
    const finish = (ok, given) => { if (answered) return; answered = true; done(ok, given); };

    if (q.type === "mcq") {
      const opts = shuffle(q.options.map((t, k) => ({ t, k })));
      box.innerHTML = `<div class="q-text">${esc(q.q)}</div><div class="options">${opts.map((o) => `<button class="opt" data-k="${o.k}">${esc(o.t)}</button>`).join("")}</div>`;
      $$(".opt", box).forEach((b) => (b.onclick = () => {
        const ok = +b.dataset.k === q.answer;
        $$(".opt", box).forEach((x) => { x.disabled = true; if (+x.dataset.k === q.answer) x.classList.add("correct"); });
        if (!ok) b.classList.add("wrong");
        finish(ok, q.options[+b.dataset.k]);
      }));
    } else if (q.type === "tf") {
      box.innerHTML = `<div class="q-text">${esc(q.q)}</div><div class="tf"><button class="opt" data-v="1">${I("check")} صح</button><button class="opt" data-v="0">${I("x")} خطأ</button></div>`;
      $$(".opt", box).forEach((b) => (b.onclick = () => {
        const v = b.dataset.v === "1", ok = v === !!q.answer;
        $$(".opt", box).forEach((x) => { x.disabled = true; if ((x.dataset.v === "1") === !!q.answer) x.classList.add("correct"); });
        if (!ok) b.classList.add("wrong");
        finish(ok, v ? "صح" : "خطأ");
      }));
    } else if (q.type === "fill") {
      const answers = [].concat(q.answer).map(norm);
      const text = esc(q.q).replace(/_{3,}/, `<span class="blank" data-blank>؟</span>`);
      const hasOpts = q.options && q.options.length;
      box.innerHTML = `<div class="q-text">${text.includes("data-blank") ? text : text + ` <span class="blank" data-blank>؟</span>`}</div>
        ${hasOpts
          ? `<div class="options">${shuffle(q.options).map((o) => `<button class="opt" data-o="${esc(o)}">${esc(o)}</button>`).join("")}</div>`
          : `<div class="fill-row"><input class="input" placeholder="اكتب الإجابة هنا..." data-in><button class="btn" data-check>${I("check")} تحقّق</button></div>`}`;
      const $blank = $("[data-blank]", box);
      const check = (given) => {
        const ok = answers.includes(norm(given));
        $blank.textContent = given || "—";
        $blank.style.color = ok ? "var(--ok)" : "var(--bad)";
        $blank.style.borderColor = ok ? "var(--ok)" : "var(--bad)";
        return ok;
      };
      if (hasOpts) {
        $$(".opt", box).forEach((b) => (b.onclick = () => {
          const ok = check(b.dataset.o);
          $$(".opt", box).forEach((x) => { x.disabled = true; if (answers.includes(norm(x.dataset.o))) x.classList.add("correct"); });
          if (!ok) b.classList.add("wrong");
          finish(ok, b.dataset.o);
        }));
      } else {
        const $in = $("[data-in]", box), $bt = $("[data-check]", box);
        const submit = () => {
          const v = $in.value.trim(); if (!v) { toast("اكتب إجابتك الأول"); return; }
          const ok = check(v); $in.disabled = true; $bt.disabled = true;
          $in.style.borderColor = ok ? "var(--ok)" : "var(--bad)";
          finish(ok, v);
        };
        $bt.onclick = submit; $in.onkeydown = (e) => { if (e.key === "Enter") submit(); };
      }
    } else if (q.type === "match") {
      const rightOrder = shuffle(q.pairs.map((p, j) => ({ t: p.b, j })));
      const link = {}; let sel = null;
      box.innerHTML = `<div class="q-text">${esc(q.q)}</div>
        <p style="color:var(--muted);margin:-8px 0 14px">اضغط على عنصر من العمود الأول وبعدين على اللي يناسبه من العمود التاني</p>
        <div class="match">
          <div class="col">${q.pairs.map((p, i) => `<button class="mi" data-a="${i}">${esc(p.a)}</button>`).join("")}</div>
          <div class="col">${rightOrder.map((r) => `<button class="mi" data-b="${r.j}">${esc(r.t)}</button>`).join("")}</div>
        </div>
        <div class="btn-row"><button class="btn" data-check disabled>${I("check")} تحقّق</button></div>`;
      const paint = () => {
        $$(".mi", box).forEach((el) => { el.removeAttribute("data-color"); el.style.background = ""; el.classList.remove("sel"); const s = el.querySelector(".pn"); if (s) s.remove(); });
        Object.keys(link).forEach((a, n) => {
          const c = MATCH_COLORS[n % MATCH_COLORS.length];
          [$(`[data-a="${a}"]`, box), $(`[data-b="${link[a]}"]`, box)].forEach((el) => { el.dataset.color = n; el.style.background = c; el.insertAdjacentHTML("beforeend", `<span class="pn">${ar(n + 1)}</span>`); });
        });
        if (sel !== null) $(`[data-a="${sel}"]`, box).classList.add("sel");
        $("[data-check]", box).disabled = Object.keys(link).length !== q.pairs.length;
      };
      $$("[data-a]", box).forEach((el) => (el.onclick = () => {
        if (answered) return; sfx.click();
        const a = el.dataset.a;
        if (link[a] !== undefined) { delete link[a]; sel = null; } else sel = sel === a ? null : a;
        paint();
      }));
      $$("[data-b]", box).forEach((el) => (el.onclick = () => {
        if (answered || sel === null) return; sfx.click();
        const b = el.dataset.b;
        Object.keys(link).forEach((k) => { if (link[k] === b) delete link[k]; });
        link[sel] = b; sel = null; paint();
      }));
      $("[data-check]", box).onclick = () => {
        let ok = true;
        q.pairs.forEach((p, i) => { const good = +link[i] === i; if (!good) ok = false; $(`[data-a="${i}"]`, box).classList.add(good ? "okm" : "badm"); });
        $("[data-check]", box).remove();
        finish(ok, ok ? "توصيل صحيح" : "توصيل غير كامل الصحة");
      };
    }
  }

  /* =====================================================================
     الشاشات
     ===================================================================== */
  const screens = {};

  /* صفوف الدروس (بتتعرض في صفحة القسم) */
  const lessonRows = (u) => `<div class="lesson-list">${u.lessons.map((l, i) => {
    const pr = progress()[l.id], d = partsDone(l);
    const st = pr ? `<span class="tag l1">${I("check")} خلصته</span>` : d ? `<span class="tag">الجزء ${ar(d)} من ${ar(l.parts.length)}</span>` : "";
    return `<button class="lesson-row ${pr ? "done" : ""}" data-lesson="${l.id}">
      <span class="num-badge">${ar(i + 1)}</span>
      <span class="grow"><b>${esc(l.title)}</b><small>${esc(l.summary)}</small></span>
      ${st}${I("next")}
    </button>`; }).join("")}</div>`;
  const bindLessons = () => $$("[data-lesson]").forEach((b) => (b.onclick = () => { sfx.click(); go("lesson", { id: b.dataset.lesson }); }));
  const unitShort = (u) => u.title.split(":")[0];
  const lessonsWord = (n) => n === 1 ? "درس واحد" : n === 2 ? "درسين" : `${ar(n)} ${n <= 10 ? "دروس" : "درس"}`;

  /* ---------- الرئيسية: الأقسام ---------- */
  screens.home = () => {
    const last = lessonById(store.get("last", ""));
    const overall = pct(doneCount(), LESSONS.length);
    render(`
      <div class="home-head">
        <div>
          <h1 class="page-title">${currentUser() ? `أهلًا يا ${esc(firstName())}` : `أهلًا بيك في ${esc(SITE.name)}`}</h1>
          <p class="page-sub">${esc(SITE.grade)} — اختار القسم اللي عايز تذاكره</p>
        </div>
        <div class="home-prog"><span>خلّصت ${ar(doneCount())} من ${lessonsWord(LESSONS.length).replace(" واحد", "")}</span><div class="progress"><span style="width:${overall}%"></span></div></div>
      </div>
      ${last ? `<div class="continue"><div style="flex:1"><small>كمّل من حيث وقفت — ${esc(last.subject.title)}</small><h3>${esc(last.title)}</h3></div><button class="btn small" data-cont>كمّل ${I("next")}</button></div>` : ""}

      <div data-units style="scroll-margin-top:130px"></div>
      <div class="section-label">${I("layers")} الأقسام</div>
      <div class="subject-grid">${SUBJECTS.map((sb) => {
        const ls = subjectLessons(sb), dn = ls.filter((l) => progress()[l.id]).length;
        return `<button class="subject-card ${ls.length ? "" : "empty"}" data-subject="${sb.id}">
          <span class="ico">${I(sb.icon)}</span>
          <span class="grow">
            <h3>${esc(sb.title)}</h3>
            <p>${ls.length ? `${sb.units.length === 1 ? "وحدة واحدة" : `${ar(sb.units.length)} وحدات`} • ${lessonsWord(ls.length)}` : "لسه مفيش محتوى — هيتضاف قريبًا"}</p>
            ${ls.length ? `<span class="progress"><span style="width:${pct(dn, ls.length)}%"></span></span>` : ""}
          </span>
          ${I("next")}
        </button>`; }).join("")}
      </div>

      <footer class="site-foot">${esc(SITE.name)} — إعداد الطالب: ${esc(SITE.student.name)} — ${esc(SITE.student.school)}<br>${esc(SITE.competition)} • HTML — CSS — JavaScript</footer>`);
    $$("[data-subject]").forEach((b) => (b.onclick = () => { sfx.click(); go("subject", { id: b.dataset.subject }); }));
    if (last) $("[data-cont]").onclick = () => { sfx.click(); go("lesson", { id: last.id }); };
  };

  /* ---------- صفحة القسم: الوحدات ← الدروس ---------- */
  screens.subject = ({ id }) => {
    const sb = subjectById(id) || SUBJECTS[0];
    render(`
      ${crumbs([{ t: sb.title }])}
      ${title(sb.icon, esc(sb.title))}
      ${sb.units.length ? sb.units.map((u) => `
        <div class="section-label">${I("layers")} ${esc(u.title)}</div>
        ${lessonRows(u)}`).join("") : `
        <div class="card empty-state">
          <span class="ico">${I(sb.icon)}</span>
          <h3>لسه مفيش محتوى — هيتضاف قريبًا</h3>
          <p>دروس ${esc(sb.title)} هتنزل هنا أول ما تتجهز.</p>
          <button class="btn soft" data-crumb="home">${I("home")} رجوع للرئيسية</button>
        </div>`}`);
    bindCrumbs(); bindLessons();
  };

  /* ---------- كلمة الختام + زرار تحميل الملف (في آخر الدرس) ---------- */
  const closingCard = (l) => {
    const c = l.closing, d = l.download;
    return `<div class="card closing">
      ${c ? `<span class="closing-ico">${I("star")}</span>
      <h3>${esc(c.title)}</h3>
      ${c.text.slice(0, 2).map((t, i) => `<p class="${i === 0 ? "closing-lead" : ""}">${esc(t)}</p>`).join("")}
      ${c.motto ? `<blockquote class="closing-motto">«${esc(c.motto)}»</blockquote>` : ""}
      ${c.text.slice(2).map((t) => `<p class="closing-sign">${esc(t)}</p>`).join("")}` : ""}
      ${d ? `<div class="dl-box" data-dl>
        <div class="dl-info">${I("file")}<div><b>${esc(d.title)}</b><small>${esc(d.desc)}</small></div></div>
        <div class="dl-container">
          <label class="dl-label" title="تحميل الملف">
            <input type="checkbox" class="dl-input" aria-label="تحميل ${esc(d.title)}">
            <span class="dl-circle"><svg class="dl-icon" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 19V5m0 14-4-4m4 4 4-4"></path></svg><span class="dl-square"></span></span>
            <span class="dl-title">تحميل</span>
            <span class="dl-title">فتح</span>
          </label>
        </div>
      </div>` : ""}
    </div>`;
  };
  // «تحميل»: الحركة بس — بعد ما تخلص يظهر «فتح»، والضغط على «فتح» هو اللي بيحمّل الملف
  function bindDownload(box, d) {
    if (!box) return;
    const lab = $(".dl-label", box), inp = $(".dl-input", box);
    const openFile = () => {
      if (!navigator.onLine) return toast("محتاج تكون متصل بالإنترنت عشان تحمّل الملف");
      sfx.pop();
      const a = document.createElement("a");
      a.href = d.file; a.target = "_blank"; a.rel = "noopener";
      document.body.appendChild(a); a.click(); a.remove();
    };
    lab.addEventListener("click", (e) => {
      if (e.target === inp) return;
      if (inp.checked) { e.preventDefault(); if (lab.classList.contains("done")) openFile(); }
    });
    inp.addEventListener("change", () => {
      if (!inp.checked) { inp.checked = true; if (lab.classList.contains("done")) openFile(); return; }
      sfx.click();
      setTimeout(() => { lab.classList.add("done"); lab.title = "فتح الملف"; }, 3600);
    });
  }

  /* ---------- كارت الامتحان النهائي في آخر الدرس ---------- */
  function examCard(l, host, closingHost) {
    const qs = qOf(l.id), e = examOf(l.id);
    const nEs = qs.filter((q) => q.type === "essay").length, nObj = qs.length - nEs;
    const total = qs.reduce((t, q) => t + marksOf(q), 0);
    let body;
    if (!qs.length) body = `<div class="placeholder">${I("info")} لسه مفيش أسئلة للدرس ده.</div>`;
    else if (examPending(e)) body = `
      <div class="exam-state wait">${I("brain")}<div><b>إجاباتك عند المصحح الذكي</b><small>بيراجعها دلوقتي — النتيجة هتظهرلك أول ما يخلص.</small></div></div>
      <div class="btn-row"><button class="btn" data-exstatus>${I("eye")} متابعة التصحيح</button></div>`;
    else if (e && e.status === "done") body = `
      <div class="exam-state ${e.passed ? "ok" : "bad"}">${I(e.passed ? "trophy" : "trend")}<div><b>آخر نتيجة: ${num(e.score)} من ${num(e.total)} (${ar(e.percent)}٪)</b>
        <small>${e.passed ? "ناجح — برافو عليك!" : "محتاج ٥٠٪ عشان تنجح — راجع الدرس وجرّب تاني."}</small></div></div>
      <div class="btn-row"><button class="btn soft" data-exstatus>${I("list")} تفاصيل التصحيح</button><button class="btn" data-exstart>${I("refresh")} امتحن تاني</button></div>`;
    else body = `
      <p class="exam-intro">امتحان على الدرس كله في صفحة لوحده: <b>${qWord(nObj)} موضوعي</b>${nEs ? ` و<b>${qWord(nEs)} مقالي</b>` : ""} — المجموع <b>${ar(total)} درجة</b>.</p>
      <ul class="exam-rules">
        <li>مفيش تصحيح أثناء الحل، جاوب على كل الأسئلة براحتك.</li>
        <li>في الآخر اضغط «إرسال الإجابات»، والمصحح الذكي هيراجعها سؤال سؤال.</li>
        <li>النتيجة بتوصلك بعد المراجعة، والنجاح من ٥٠٪.</li>
      </ul>
      <div class="btn-row"><button class="btn" data-exstart>${I("play")} ابدأ الامتحان</button></div>`;
    host.innerHTML = `<h3 class="block-title">${I("pencil")} الامتحان النهائي</h3>${body}`;
    const st = $("[data-exstart]", host), sv = $("[data-exstatus]", host);
    if (st) st.onclick = () => { sfx.click(); go("exam", { id: l.id }); };
    if (sv) sv.onclick = () => { sfx.click(); go("examStatus", { id: l.id }); };
    // كلمة الختام وزرار التحميل بيظهروا بس بعد النجاح في الامتحان
    if (e && e.ever && (l.closing || l.download)) {
      closingHost.innerHTML = closingCard(l);
      if (l.download) bindDownload($("[data-dl]", closingHost), l.download);
    } else closingHost.innerHTML = "";
  }

  /* =====================================================================
     الدرس (صفحة واحدة بتنزل لتحت):
     أهداف الدرس ← الجزء ١ ← الجزء ٢ ... ← الملخص ← الامتحان النهائي (في صفحة لوحده)
     كل جزء: حوار ← مثال ← تطبيق عملي ← تحقق من النقاط الأساسية
     الجزء اللي بعده بيظهر لوحده أول ما تجاوب أسئلة التحقق صح
     ===================================================================== */
  screens.lesson = ({ id }) => {
    setTimeout(aiWarm, 2500); // المصحح بيتجهز في الخلفية عشان «تحقق من إجابتك» والامتحان يبقوا أسرع
    const l = lessonById(id), idx = LESSONS.indexOf(l), n = l.parts.length;
    const prevL = LESSONS[idx - 1], nextL = LESSONS[idx + 1];
    store.set("last", id);
    render(`
      ${crumbs([{ t: l.subject.title, go: ["subject", { id: l.subject.id }] }, { t: unitShort(l.unit), go: ["subject", { id: l.subject.id }] }, { t: l.title }])}
      ${title("book", esc(l.title))}
      <p class="page-sub">${esc(l.summary)}</p>
      ${(l.objectives || []).length ? `<div class="card"><h3 class="block-title">${I("target")} هتتعلم في الدرس ده</h3><ul class="obj-list">${l.objectives.map((o) => `<li>${esc(o)}</li>`).join("")}</ul></div>` : ""}
      <div data-parts></div>
      <div data-end></div>
      <div class="step-nav">
        ${prevL ? `<button class="btn soft" data-lesson="${prevL.id}">${I("back")} الدرس السابق</button>` : "<span></span>"}
        ${nextL ? `<button class="btn soft" data-lesson="${nextL.id}">الدرس التالي ${I("next")}</button>` : "<span></span>"}
      </div>`);
    bindCrumbs();
    $$("[data-lesson]").forEach((b) => (b.onclick = () => { sfx.click(); go("lesson", { id: b.dataset.lesson }); }));

    const $parts = $("[data-parts]"), $end = $("[data-end]");
    let opening = true; // وقت فتح الصفحة: الأجزاء المحلولة قبل كده بتظهر من غير ما الصفحة تنزل

    // الملخص + الامتحان النهائي (بيظهروا بعد آخر جزء)
    const showEnd = () => {
      if ($end.dataset.on) return; $end.dataset.on = "1";
      $end.innerHTML = `
        ${(l.keypoints || []).length ? `<div class="card block"><h3 class="block-title">${I("list")} ملخص الدرس</h3><ul class="key-list">${l.keypoints.map((k) => `<li>${esc(k)}</li>`).join("")}</ul></div>` : ""}
        <div class="card block exam-card" data-practice></div>
        <div data-closing></div>`;
      examCard(l, $("[data-practice]", $end), $("[data-closing]", $end));
    };

    const addPart = (i) => {
      if (i >= n) { showEnd(); if (!opening) setTimeout(() => $end.scrollIntoView({ behavior: "smooth", block: "start" }), 700); return; }
      const box = document.createElement("div");
      $parts.appendChild(box);
      renderPart(l, i, box, () => addPart(i + 1));
      if (!opening) setTimeout(() => box.scrollIntoView({ behavior: "smooth", block: "start" }), 700);
    };
    addPart(0);
    opening = false;
  };

  /* ---------- جزء من الدرس ---------- */
  function renderPart(l, pi, host, onPassed) {
    const part = l.parts[pi], chars = SITE.characters, already = cpPassed(l.id, pi);
    const dlg = part.dialogue || [];
    host.innerHTML = `
      <div class="card">
        <h2 class="part-title">${I("chat", 'style="width:28px;height:28px;color:var(--coffee)"')} ${esc(part.title)}</h2>
        <div class="chat" data-chat></div>
      </div>
      <div data-after></div>`;
    const $chat = $("[data-chat]", host);
    const bubble = (m, k) => `
      <div class="msg ${m.who === "student" ? "student" : "teacher"}">
        ${AVATARS[m.who === "student" ? "student" : "teacher"]}
        <div class="bubble"><span class="who">${esc(chars[m.who === "student" ? "student" : "teacher"].name)}<button class="say" data-say="${k}" title="استمع">${I("volume", 'style="width:15px;height:15px"')}</button></span>${esc(m.text)}</div>
      </div>`;
    const bindSay = () => $$("[data-say]", $chat).forEach((b) => (b.onclick = () => speak(dlg[+b.dataset.say].text)));


    const renderAfter = () => {
      const $after = $("[data-after]", host);
      let html = "";
      // الأمثلة: نص، أو اقتباس (زي مواد الدستور)، أو جدول — وممكن أكتر من مثال في الجزء
      const exList = part.example ? (Array.isArray(part.example) ? part.example : [part.example]) : [];
      exList.forEach((ex) => {
        let inner = "";
        if (ex.text) inner += `<div class="example-box">${esc(ex.text)}</div>`;
        if (ex.quote) inner += `<blockquote class="quote-box">«${esc(ex.quote)}»${ex.source ? `<cite>${esc(ex.source)}</cite>` : ""}</blockquote>`;
        if (ex.img) inner += `<figure class="lesson-fig"><img src="${esc(ex.img)}" alt="${esc(ex.caption || ex.title || "")}" loading="lazy">${ex.caption ? `<figcaption>${esc(ex.caption)}</figcaption>` : ""}</figure>`;
        if (ex.table) inner += `<div class="tbl-wrap"><table class="lesson-tbl">
            ${ex.table.head ? `<thead><tr>${ex.table.head.map((h) => `<th>${esc(h)}</th>`).join("")}</tr></thead>` : ""}
            <tbody>${ex.table.rows.map((r) => `<tr>${r.map((c, ci) => `<td>${ci === 0 && ex.table.head ? `<b>${esc(c)}</b>` : esc(c)}</td>`).join("")}</tr>`).join("")}</tbody></table></div>`;
        html += `<div class="block card"><h3 class="block-title">${I(ex.img ? "image" : ex.table ? "list" : ex.quote ? "book" : "bulb")} ${esc(ex.title || "مثال توضيحي")}</h3>${inner}</div>`;
      });
      if (part.task) {
        const t = part.task, runnable = ["html", "js", "css"].includes(t.lang), isText = !t.lang || t.lang === "text";
        html += `
        <div class="block card" data-task>
          <h3 class="block-title">${I(isText ? "pencil" : "code")} التطبيق العملي</h3>
          <ol class="task-steps">${(t.steps || []).map((s) => `<li>${esc(s)}</li>`).join("")}</ol>
          ${isText
            ? `<textarea class="input answer-area" data-code placeholder="${esc(t.placeholder || "اكتب إجابتك هنا...")}">${esc(t.code || "")}</textarea>`
            : `<div class="code-wrap">
            <div class="code-head"><span>منطقة التطبيق — ${esc(t.lang.toUpperCase())}</span><span class="dots"><span></span><span></span><span></span></span></div>
            <textarea class="code-area" data-code spellcheck="false">${esc(t.code || "")}</textarea>
          </div>`}
          <div class="btn-row">
            ${runnable ? `<button class="btn" data-run>${I("play")} تشغيل</button>` : ""}
            ${t.check && t.check.length ? `<button class="btn ${runnable ? "soft" : ""}" data-verify>${I("check")} تحقّق من إجابتك</button>` : ""}
            ${t.hints && t.hints.length ? `<button class="btn soft" data-hint>${I("bulb")} تلميح (${ar(t.hints.length)})</button>` : ""}
            ${t.model ? `<button class="btn soft" data-model>${I("eye")} الإجابة النموذجية</button>` : ""}
            ${isText ? "" : `<button class="btn soft" data-reset>${I("refresh")} إعادة الكود</button>`}
          </div>
          <div data-hints></div><div data-verify-out></div><div data-model-out></div><div data-out></div>
        </div>`;
      }
      html += `
        <div class="block checkpoint" data-cp>
          <h3 class="block-title">${I("shield")} تحقق من النقاط الأساسية</h3>
          <p style="color:var(--muted);margin-top:-6px">جاوب صح على الأسئلة دي عشان يظهرلك الجزء اللي بعده.</p>
          ${(part.checkpoint || []).map((_, k) => `<div class="cp-q"><div class="cp-num">${I("flag")} سؤال ${ar(k + 1)}</div><div data-cpq="${k}"></div><div data-cpfb="${k}"></div></div>`).join("")}
          <div data-cpdone></div>
        </div>`;
      $after.innerHTML = html;

      // التطبيق العملي
      if (part.task) {
        const t = part.task, $code = $("[data-code]", $after);
        $code.onkeydown = (e) => { if (e.key === "Tab") { e.preventDefault(); const s = $code.selectionStart; $code.setRangeText("  ", s, $code.selectionEnd, "end"); } };
        let hintI = 0;
        const $h = $("[data-hint]", $after);
        if ($h) $h.onclick = () => {
          if (hintI >= t.hints.length) return;
          sfx.pop(); $("[data-hints]", $after).insertAdjacentHTML("beforeend", `<div class="hint">${I("bulb")}<span><b>تلميح ${ar(hintI + 1)}:</b> ${esc(t.hints[hintI])}</span></div>`);
          hintI++; if (hintI >= t.hints.length) { $h.disabled = true; }
        };
        const $rs = $("[data-reset]", $after);
        if ($rs) $rs.onclick = () => { $code.value = t.code || ""; sfx.click(); };
        const $md = $("[data-model]", $after);
        if ($md) $md.onclick = () => {
          sfx.pop(); $md.disabled = true;
          $("[data-model-out]", $after).innerHTML = `<div class="model-box"><b>${I("check")} الإجابة النموذجية:</b><div>${esc(t.model).replace(/\n/g, "<br>")}</div></div>`;
        };
        const $run = $("[data-run]", $after);
        if ($run) $run.onclick = () => {
          sfx.click();
          const src = $code.value;
          let doc;
          if (t.lang === "html") doc = src;
          else if (t.lang === "css") doc = `<style>${src}</style><h1>عنوان تجريبي</h1><p>فقرة تجريبية لتجربة التنسيق.</p><button>زرار</button>`;
          else doc = `<pre id="o" style="font:14px Consolas,monospace;white-space:pre-wrap;direction:ltr;text-align:left;margin:0;padding:10px"></pre><script>
            const o=document.getElementById('o');const w=(...a)=>{o.textContent+=a.map(x=>typeof x==='object'?JSON.stringify(x):String(x)).join(' ')+'\\n'};
            console.log=w;window.alert=w;window.onerror=(m)=>{o.textContent+='خطأ: '+m+'\\n'};
            try{${src.replace(/<\/script/gi, "<\\/script")}}catch(e){o.textContent+='خطأ: '+e.message}<\/script>`;
          $("[data-out]", $after).innerHTML = `<div class="block-title" style="margin-top:16px;font-size:1rem">${I("eye")} النتيجة</div><iframe class="run-out" sandbox="allow-scripts"></iframe>`;
          $("[data-out] iframe", $after).srcdoc = `<!DOCTYPE html><html dir="rtl"><head><meta charset="utf-8"><style>body{font-family:Tahoma,Arial,sans-serif}</style></head><body>${doc}</body></html>`;
        };
        const $v = $("[data-verify]", $after);
        if ($v) $v.onclick = async () => {
          // المصحح الذكي بيفهم الإجابة حتى لو من غير تشكيل أو همزات أو بأسلوب الطالب
          const out = $("[data-verify-out]", $after);
          if (!$code.value.trim()) { sfx.bad(); out.innerHTML = `<div class="feedback bad">${I("x")} اكتب إجابتك الأول.</div>`; return; }
          $v.disabled = true;
          out.innerHTML = `<div class="feedback info thinking">${I("brain")} المصحح الذكي بيقرأ إجابتك<span class="dots"><i></i><i></i><i></i></span></div>`;
          try {
            const r = await api("check", { lesson: l.id, part: pi, text: $code.value });
            if (r.ok) { sfx.win(); out.innerHTML = `<div class="praise">${I("trophy")} ${esc(r.feedback)}</div>`; }
            else { sfx.bad(); out.innerHTML = `<div class="feedback bad">${I("x")} قربت! ${esc(r.feedback)} — جرّب تستخدم التلميحات.</div>`; }
          } catch (err) {
            // من غير المصحح مش هنحكم على الإجابة (عشان منقولش على إجابة صح إنها غلط)
            out.innerHTML = `<div class="feedback info">${I("info")} المصحح الذكي مش متاح دلوقتي (محتاج إنترنت أول مرة عشان يتحمّل)، فمش هقدر أحكم على إجابتك. قارنها بنفسك بالإجابة النموذجية وجرّب تاني بعد شوية.</div>
              <div class="model-box"><b>${I("check")} الإجابة النموذجية:</b><div>${esc(t.model || "").replace(/\n/g, "<br>")}</div></div>`;
          }
          $v.disabled = false;
        };
      }

      // أسئلة التحقق
      const cpList = part.checkpoint || [];
      const solved = new Array(cpList.length).fill(already);
      const allDone = () => {
        const box = $("[data-cpdone]", $after);
        if (!box || box.dataset.done) return; box.dataset.done = "1";
        if (!already) { setCp(l.id, pi); sfx.win(); }
        box.innerHTML = `<div class="praise">${I("trophy")}<div>لقد فعلتها! خلّصت ${esc(part.title)}<div style="font-weight:400;font-size:.95rem">كل النقاط الأساسية واضحة — الجزء اللي بعده ظهر تحت.</div></div></div>`;
        onPassed();
      };
      const ask = (k) => {
        const q = cpList[k], qbox = $(`[data-cpq="${k}"]`, $after), fb = $(`[data-cpfb="${k}"]`, $after);
        fb.innerHTML = "";
        renderQ(q, qbox, (ok) => {
          recordAnswer(ok);
          if (ok) {
            sfx.ok(); solved[k] = true;
            fb.innerHTML = `<div class="feedback ok">${I("check")} ${praise()}${q.explain ? ` <span style="font-weight:400">— ${esc(q.explain)}</span>` : ""}</div>`;
            if (solved.every(Boolean)) allDone();
          } else {
            sfx.bad();
            fb.innerHTML = `<div class="feedback bad">${I("x")} مش مظبوطة، فكّر تاني!${q.explain ? ` <span style="font-weight:400">— ${esc(q.explain)}</span>` : ""}</div>
              <div class="btn-row"><button class="btn soft small" data-retry="${k}">${I("refresh")} حاول مرة تانية</button></div>`;
            $(`[data-retry="${k}"]`, $after).onclick = () => { sfx.click(); ask(k); };
          }
        });
      };
      cpList.forEach((_, k) => ask(k));
      if (!cpList.length || already) allDone();
    };

    // الحوار كله بيظهر مرة واحدة، وبعده المثال والتطبيق وأسئلة التحقق
    $chat.innerHTML = dlg.map(bubble).join("");
    bindSay();
    renderAfter();
  }

  /* =====================================================================
     الامتحان النهائي — صفحة مقفولة لوحدها
     مفيش تصحيح أثناء الحل؛ الإجابات بتتبعت للمصحح الذكي مرة واحدة في الآخر
     ===================================================================== */
  function examQ(q, n) {
    let body = "";
    if (q.type === "mcq") body = `<div class="options">${shuffle(q.options.map((t, k) => ({ t, k }))).map((o) => `<button class="opt" data-pick="${o.k}">${esc(o.t)}</button>`).join("")}</div>`;
    else if (q.type === "tf") body = `<div class="tf"><button class="opt" data-pick="1">${I("check")} صح</button><button class="opt" data-pick="0">${I("x")} خطأ</button></div>`;
    else if (q.type === "fill") body = q.options && q.options.length
      ? `<div class="options">${shuffle(q.options).map((o) => `<button class="opt" data-pick="${esc(o)}">${esc(o)}</button>`).join("")}</div>`
      : `<input class="input" data-text placeholder="اكتب الإجابة هنا...">`;
    else if (q.type === "match") {
      const right = shuffle(q.pairs.map((p, j) => ({ t: p.b, j })));
      body = `<div class="ex-match">${q.pairs.map((p, i) => `<label class="ex-mrow"><span>${esc(p.a)}</span>
        <select class="input" data-m="${i}"><option value="">اختار...</option>${right.map((r) => `<option value="${r.j}">${esc(r.t)}</option>`).join("")}</select></label>`).join("")}</div>`;
    } else if (q.type === "essay") body = `<textarea class="input answer-area" data-text placeholder="اكتب إجابتك بأسلوبك..."></textarea><small class="ex-wc" data-wc>٠ كلمة</small>`;
    const qt = q.type === "fill" ? esc(q.q).replace(/_{3,}/, `<span class="blank">.......</span>`) : esc(q.q);
    return `<div class="card ex-q" data-exq="${q.id}">
      <div class="ex-top"><span class="ex-num">${ar(n + 1)}</span>${typeTag(q.type)}<span class="tag">${marksWord(marksOf(q))}</span></div>
      <div class="q-text">${qt}</div>${body}</div>`;
  }

  function graderOffline(retry) {
    modal(`<div class="big-ico bad">${I("power")}</div><h3>${LOCAL ? "المصحح الذكي مش شغال" : "المصحح الذكي مش متاح دلوقتي"}</h3>
      ${LOCAL ? `<p style="color:var(--muted)">عشان إجاباتك تتصحح لازم برنامج المصحح يكون شغال على الجهاز:</p>
      <ol class="offline-steps">
        <li>افتح فولدر المنصة.</li>
        <li>شغّل ملف <b dir="ltr">ai_grader.py</b> (دبل كليك عليه، أو اكتب <b dir="ltr">python ai_grader.py</b>).</li>
        <li>ارجع هنا واضغط «حاول تاني».</li>
      </ol>` : `<p style="color:var(--muted)">ممكن يكون المصحح بيصحى بعد فترة من غير استخدام، وده بياخد حوالي دقيقة. استنى شوية واضغط «حاول تاني».</p>`}
      <p style="color:var(--muted)">إجاباتك لسه موجودة في الصفحة، مش هتضيع.</p>
      <div class="btn-row" style="justify-content:center"><button class="btn" data-y>${I("refresh")} حاول تاني</button><button class="btn soft" data-n>إلغاء</button></div>`,
      (m, close) => { m.querySelector("[data-n]").onclick = close; m.querySelector("[data-y]").onclick = () => { close(); retry(); }; });
  }

  screens.exam = ({ id }) => {
    aiWarm();
    const l = lessonById(id);
    if (examPending(examOf(id))) return go("examStatus", { id }, false);
    const all = qOf(id);
    const qs = [...shuffle(all.filter((q) => q.type !== "essay")), ...all.filter((q) => q.type === "essay")];
    const total = qs.reduce((t, q) => t + marksOf(q), 0), ans = {};
    render(`
      <div class="exam-head">
        <div class="grow"><small>${esc(l.subject.title)} › ${esc(unitShort(l.unit))}</small>
          <h1>${I("lock")} الامتحان النهائي: ${esc(l.title)}</h1>
          <p>${qWord(qs.length)} — ${ar(total)} درجة • مفيش تصحيح أثناء الحل، والنتيجة بتوصلك بعد مراجعة المصحح الذكي</p></div>
        <button class="btn soft small" data-exquit>${I("logout")} خروج</button>
      </div>
      <div class="exam-list">${qs.map(examQ).join("")}</div>
      <div class="exam-bar">
        <div class="grow"><b data-excount></b><div class="progress"><span data-exprog style="width:0"></span></div></div>
        <button class="btn" data-exsend>${I("send")} إرسال الإجابات</button>
      </div>`);
    window.onbeforeunload = (e) => { e.preventDefault(); e.returnValue = ""; };
    const answered = (q) => {
      const v = ans[q.id];
      if (v === undefined || v === null) return false;
      if (q.type === "match") return Object.keys(v).length === q.pairs.length;
      return typeof v === "string" ? v.trim().length > 0 : true;
    };
    const count = () => {
      const d = qs.filter(answered).length;
      $("[data-excount]").textContent = `أجبت على ${ar(d)} من ${ar(qs.length)}`;
      $("[data-exprog]").style.width = pct(d, qs.length) + "%";
      return d;
    };
    $$("[data-exq]").forEach((box) => {
      const q = qs.find((x) => x.id === box.dataset.exq);
      const mark = () => { box.classList.toggle("done", answered(q)); count(); };
      $$("[data-pick]", box).forEach((b) => (b.onclick = () => {
        sfx.click();
        $$("[data-pick]", box).forEach((x) => x.classList.toggle("picked", x === b));
        const v = b.dataset.pick;
        ans[q.id] = q.type === "mcq" ? +v : q.type === "tf" ? v === "1" : v;
        mark();
      }));
      const t = $("[data-text]", box);
      if (t) t.oninput = () => {
        ans[q.id] = t.value;
        if (q.type === "essay") $("[data-wc]", box).textContent = `${ar(t.value.trim() ? t.value.trim().split(/\s+/).length : 0)} كلمة`;
        mark();
      };
      $$("[data-m]", box).forEach((sl) => (sl.onchange = () => {
        const m = ans[q.id] || {};
        if (sl.value === "") delete m[sl.dataset.m]; else m[sl.dataset.m] = sl.value;
        ans[q.id] = m; mark();
      }));
    });
    count();
    $("[data-exquit]").onclick = () => confirmBox("عايز تخرج من الامتحان؟", "إجاباتك مش هتتحفظ، وتقدر تبدأ الامتحان من جديد في أي وقت.", "أيوه، اخرج", () => go("lesson", { id }, false));
    const $send = $("[data-exsend]");
    const doSend = async () => {
      $send.disabled = true; $send.innerHTML = `${I("send")} بيتبعت...`;
      try {
        const r = await api("submit", { lesson: id, user: session || "", answers: qs.map((q) => ({ id: q.id, value: answered(q) ? ans[q.id] : null })) });
        const prev = examOf(id) || {};
        setExam(id, { sid: r.id, status: "queued", at: Date.now(), ever: !!prev.ever });
        sfx.ok(); go("examStatus", { id }, false);
      } catch (err) {
        $send.disabled = false; $send.innerHTML = `${I("send")} إرسال الإجابات`;
        err.server ? toast(err.message) : graderOffline(doSend);
      }
    };
    $send.onclick = () => {
      sfx.click();
      const miss = qs.length - count();
      if (miss) confirmBox("لسه في أسئلة مجاوبتش عليها", `فاضل ${qWord(miss)} من غير إجابة، وهيتحسبوا صفر. تبعت إجاباتك برضه؟`, "أيوه، ابعت", doSend);
      else confirmBox("تبعت إجاباتك؟", "بعد الإرسال مش هتقدر تغيّر إجاباتك، والمصحح الذكي هيبدأ يراجعها.", "أيوه، ابعت", doSend);
    };
  };

  /* ---------- نتيجة الامتحان: بتستنى المصحح لحد ما يخلص ---------- */
  const STATE = { correct: ["ok", "صح"], partial: ["part", "درجة جزئية"], wrong: ["bad", "غلط"], empty: ["bad", "لم تُجب"] };
  function reviewItem(it, n) {
    const [cls, lbl] = STATE[it.state] || STATE.wrong;
    let body;
    if (it.type === "essay") body = `
      <div class="rv-ans"><small>إجابتك:</small><div>${esc(it.given).replace(/\n/g, "<br>")}</div></div>
      <div class="rv-fb">${I("brain")}<span><b>ملاحظة المصحح:</b> ${esc(it.feedback)}</span></div>
      ${it.review ? `<div class="rv-review">${I("eye")}<span><b>محتاج مراجعة المعلم:</b> ${esc(it.review)}</span></div>` : ""}
      ${(it.found || []).length || (it.missing || []).length || (it.partial || []).length ? `<ul class="rv-points">${(it.found || []).map((p) => `<li class="ok">${I("check")}<span>${esc(p)}</span><small>موجودة</small></li>`).join("")}${(it.partial || []).map((p) => `<li class="part">${I("info")}<span>${esc(p)}</span><small>موجودة جزئيًا</small></li>`).join("")}${(it.missing || []).map((p) => `<li class="bad">${I("x")}<span>${esc(p)}</span><small>ناقصة</small></li>`).join("")}</ul>` : ""}
      <div class="model-box"><b>${I("check")} الإجابة النموذجية:</b><div>${esc(it.model)}</div></div>
      <details class="rv-think"><summary>${I("sparkle")} إزاي المصحح فكّر؟</summary>
        <ol>${(it.thoughts || []).map((t) => `<li>${esc(t)}</li>`).join("")}</ol>
        <small>ثقة المصحح في الدرجة: ${esc(it.confidence || "")}</small></details>`;
    else body = `<small>إجابتك: <b>${esc(it.given)}</b>${it.state === "correct" ? "" : ` — الصح: <b>${esc(it.correct_text)}</b>`}</small>
      ${it.explain ? `<small>${I("bulb")} ${esc(it.explain)}</small>` : ""}`;
    return `<div class="rv ${cls}">
      <div class="rv-head"><span>${ar(n + 1)}. ${TYPES[it.type] || ""}</span><span class="tag rv-${cls}">${lbl} — ${num(it.score)} من ${num(it.marks)}</span></div>
      <div class="rv-q">${esc(it.q)}</div>${body}</div>`;
  }

  screens.examStatus = ({ id }) => {
    const l = lessonById(id), idx = LESSONS.indexOf(l), nextL = LESSONS[idx + 1];
    render(`
      ${crumbs([{ t: l.subject.title, go: ["subject", { id: l.subject.id }] }, { t: l.title, go: ["lesson", { id }] }, { t: "نتيجة الامتحان" }])}
      ${title("sparkle", "نتيجة الامتحان النهائي")}
      <p class="page-sub">${esc(l.title)}</p>
      <div data-exout></div><div data-closing></div>`);
    bindCrumbs();
    const out = $("[data-exout]"), e0 = examOf(id);
    const again = () => { sfx.click(); go("exam", { id }); };
    if (!e0 || !e0.sid) {
      out.innerHTML = `<div class="card empty-state"><span class="ico">${I("pencil")}</span><h3>لسه ما امتحنتش الدرس ده</h3>
        <button class="btn" data-again>${I("play")} ابدأ الامتحان</button></div>`;
      $("[data-again]").onclick = again; return;
    }
    const showResult = (res, fresh) => {
      const p = res.percent, e = examOf(id) || {};
      if (fresh) p >= PASS ? sfx.win() : sfx.bad();
      out.innerHTML = `
        <div class="card result">
          <div class="big-ico ${res.passed ? "ok" : "bad"}">${I(p === 100 ? "trophy" : res.passed ? "star" : "trend")}</div>
          <div class="score">${num(res.score)} / ${num(res.total)}</div>
          <p><b>${ar(p)}٪</b> — ${res.passed ? (p >= 85 ? "ممتاز جدًا! لقد فعلتها!" : "ناجح — خطوة ممتازة!") : "محتاج ٥٠٪ عشان تنجح — راجع الدرس وجرّب تاني"}</p>
          <p class="reviewer">${I("brain")} صحّحه: ${esc(res.reviewer || "المصحح الذكي")}</p>
          ${(() => { const n = res.items.filter((x) => x.review).length; return n ? `<p class="review-note">${I("eye")} ${n === 1 ? "في إجابة مقالية محتاجة" : n === 2 ? "في إجابتين مقاليتين محتاجين" : `في ${ar(n)} إجابات مقالية محتاجة`} مراجعة المعلم — درجتها مبدئية، وتفاصيلها تحت.</p>` : ""; })()}
          <div class="btn-row" style="justify-content:center">
            <button class="btn soft" data-back>${I("back")} رجوع للدرس</button>
            <button class="btn ${res.passed ? "soft" : ""}" data-again>${I("refresh")} امتحن تاني</button>
            ${res.passed && nextL ? `<button class="btn" data-nextl>الدرس التالي ${I("next")}</button>` : ""}
          </div>
        </div>
        <div class="section-label">${I("list")} مراجعة الإجابات</div>
        <div class="review">${res.items.map(reviewItem).join("")}</div>`;
      $("[data-back]").onclick = () => { sfx.click(); go("lesson", { id }); };
      $("[data-again]").onclick = again;
      const nb = $("[data-nextl]"); if (nb) nb.onclick = () => { sfx.click(); go("lesson", { id: nextL.id }); };
      if (e.ever && (l.closing || l.download)) {
        const ch = $("[data-closing]"); ch.innerHTML = closingCard(l);
        if (l.download) bindDownload($("[data-dl]", ch), l.download);
      }
    };
    if (e0.status === "done" && e0.result) return showResult(e0.result, false);

    out.innerHTML = `<div class="card status-card">
      <div class="grader-anim">${I("brain")}<span class="dots"><i></i><i></i><i></i></span></div>
      <h3 data-st-title>بنتواصل مع المصحح الذكي...</h3><p data-st-sub></p>
      <div class="progress"><span data-st-bar style="width:0"></span></div>
      <small>تقدر تسيب الصفحة دي وترجع بعدين — النتيجة هتفضل محفوظة.</small></div>`;
    const setS = (t, sub, w) => { $("[data-st-title]").textContent = t; $("[data-st-sub]").textContent = sub; if (w !== undefined) $("[data-st-bar]").style.width = w + "%"; };
    let busy = false;
    const tick = async () => {
      if (busy || current.name !== "examStatus") return; busy = true;
      try {
        const r = await api("result/" + e0.sid);
        if (current.name !== "examStatus" || current.params.id !== id) return;
        if (r.status === "done" && r.result) {
          clearTimers();
          const res = r.result;
          const prev = examOf(id) || {};
          setExam(id, { sid: e0.sid, status: "done", at: prev.at, score: res.score, total: res.total, percent: res.percent,
            passed: res.passed, ever: !!prev.ever || res.passed, result: res });
          if (res.passed) saveLesson(id, res.score, res.total);
          showResult(res, true);
        } else if (r.status === "error") {
          clearTimers(); setExam(id, Object.assign({}, examOf(id), { status: "error" }));
          out.innerHTML = `<div class="card empty-state"><span class="ico">${I("alert")}</span><h3>حصلت مشكلة أثناء التصحيح</h3><p>ابعت الامتحان تاني.</p><button class="btn" data-again>${I("refresh")} امتحن تاني</button></div>`;
          $("[data-again]").onclick = again;
        } else {
          const pr = r.progress || { done: 0, total: 1 };
          setExam(id, Object.assign({}, examOf(id), { status: r.status }));
          if (r.note) setS(r.noteTitle, r.note, 3);
          else if (r.status === "queued") setS("امتحانك في الطابور", r.ahead ? `قدامك ${ar(r.ahead)} امتحان — المصحح هيوصلك حالًا.` : "المصحح هيبدأ فيه حالًا.", 3);
          else setS(`المصحح بيراجع السؤال ${ar(Math.min(pr.done + 1, pr.total))} من ${ar(pr.total)}`,
            pr.current === "essay" ? "بيقرأ إجابتك المقالية ويقارنها بأفكار الدرس..." : "بيراجع إجاباتك واحدة واحدة...", Math.max(3, pct(pr.done, pr.total)));
        }
      } catch (err) {
        if (err.server) {
          clearTimers(); setExam(id, Object.assign({}, examOf(id), { status: "error" }));
          out.innerHTML = `<div class="card empty-state"><span class="ico">${I("alert")}</span><h3>الامتحان ده مش موجود عند المصحح</h3><p>ممكن يكون ملف النتائج اتمسح. ابعت الامتحان تاني.</p><button class="btn" data-again>${I("refresh")} امتحن تاني</button></div>`;
          $("[data-again]").onclick = again;
        } else setS("مستني المصحح الذكي", "المصحح مش متاح دلوقتي — التصحيح هيكمل لوحده أول ما يرجع.");
      } finally { busy = false; }
    };
    tick(); timers.push(setInterval(tick, 2500));
  };

  /* ---------- الوداع والخروج ---------- */
  screens.bye = () => {
    render(`
      <div class="splash"><div>
        ${cupSVG("cup-big")}
        <h1>شكرًا لزيارتك</h1>
        <p class="tagline">نتمنى تكون استفدت — نشوفك قريب!</p>
        <p class="comp">تقدر تقفل الصفحة دلوقتي بأمان.</p>
        <button class="btn soft" data-ret>الرجوع للمنصة</button>
      </div></div>`);
    $("[data-ret]").onclick = () => { history.length = 0; go("home", {}, false); };
  };
  function askExit() {
    sfx.click();
    modal(`<div class="big-ico">${I("logout")}</div><h3>عايز تخرج من المنصة؟</h3><p style="color:var(--muted)">تقدّمك محفوظ، وتقدر ترجع في أي وقت.</p>
      <div class="btn-row" style="justify-content:center"><button class="btn" data-y>أيوه، خروج</button><button class="btn soft" data-n>لا، هكمّل</button></div>`,
      (m, close) => { m.querySelector("[data-n]").onclick = close; m.querySelector("[data-y]").onclick = () => { close(); window.close(); go("bye", {}, false); }; });
  }

  /* =====================================================================
     تسجيل الدخول / إنشاء حساب
     (الحسابات بتتحفظ في المتصفح على نفس الجهاز — مش محتاجة إنترنت)
     ===================================================================== */
  const users = () => store.get("users", {});
  const hashPw = (s) => { let h = 5381; for (const c of String(s)) h = ((h * 33) ^ c.charCodeAt(0)) >>> 0; return h.toString(36); };
  const currentUser = () => (session ? users()[session] : null);
  const firstName = () => { const u = currentUser(); return u ? u.name.split(" ")[0] : ""; };
  const field = (label, name, type, ph) =>
    `<div class="field"><label>${label}</label><input class="input" name="${name}" type="${type}" placeholder="${ph}" autocomplete="off"></div>`;

  function paintUser() {
    const u = currentUser();
    $user.classList.toggle("in", !!u);
    $userLbl.textContent = u ? firstName() : "تسجيل الدخول";
    $user.setAttribute("aria-label", u ? `حساب ${u.name}` : "تسجيل الدخول");
    $user.title = u ? u.name : "تسجيل الدخول / إنشاء حساب";
  }
  function setSession(name) {
    session = name;
    name ? store.set("user", name) : store.del("user");
    paintUser();
    history.length = 0; go("home", {}, false);
  }

  function authModal(tab = "login") {
    sfx.click();
    modal(`<div class="auth">
        <div class="big-ico" data-ico></div>
        <h3 data-ttl></h3>
        <div class="auth-tabs"><button type="button" data-tab="login">تسجيل الدخول</button><button type="button" data-tab="signup">إنشاء حساب</button></div>
        <form data-form novalidate></form>
        <div class="auth-switch" data-sw></div>
      </div>`, (m, close) => {
      const $f = m.querySelector("[data-form]");
      const show = (t) => {
        tab = t;
        m.querySelectorAll("[data-tab]").forEach((b) => b.classList.toggle("on", b.dataset.tab === t));
        m.querySelector("[data-ico]").innerHTML = I(t === "login" ? "user" : "userPlus");
        m.querySelector("[data-ttl]").textContent = t === "login" ? "أهلًا بيك تاني" : "اعمل حساب جديد";
        $f.innerHTML =
          (t === "signup" ? field("الاسم بالكامل", "name", "text", "مثال: أحمد محمد") : "") +
          field("اسم المستخدم", "user", "text", "حروف إنجليزي أو أرقام") +
          field("كلمة السر", "pass", "password", "٤ حروف أو أرقام على الأقل") +
          (t === "signup" ? field("تأكيد كلمة السر", "pass2", "password", "اكتبها تاني") : "") +
          `<div class="auth-err" data-err></div>
           <button class="btn" type="submit">${t === "login" ? `${I("login")} دخول` : `${I("check")} إنشاء الحساب`}</button>`;
        m.querySelector("[data-sw]").innerHTML = t === "login"
          ? `معندكش حساب؟ <button type="button" data-to="signup">إنشاء حساب</button>`
          : `عندك حساب؟ <button type="button" data-to="login">تسجيل الدخول</button>`;
        m.querySelector("[data-to]").onclick = (e) => { sfx.click(); show(e.target.dataset.to); };
        $f.querySelector("input").focus();
      };
      m.querySelectorAll("[data-tab]").forEach((b) => (b.onclick = () => { sfx.click(); show(b.dataset.tab); }));

      $f.onsubmit = (e) => {
        e.preventDefault();
        const val = (n) => { const x = $f.querySelector(`[name="${n}"]`); return x ? x.value : ""; };
        const err = (msg) => { sfx.bad(); $f.querySelector("[data-err]").textContent = msg; };
        const U = users(), un = val("user").trim().toLowerCase(), pw = val("pass");
        if (tab === "login") {
          if (!un || !pw) return err("اكتب اسم المستخدم وكلمة السر");
          if (!U[un]) return err("الحساب ده مش موجود — اعمل حساب جديد");
          if (U[un].pass !== hashPw(pw)) return err("كلمة السر غلط");
          close(); setSession(un); sfx.win(); toast(`أهلًا يا ${firstName()}`);
        } else {
          const name = val("name").trim().replace(/\s+/g, " ");
          if (name.length < 3) return err("اكتب اسمك بالكامل");
          if (!/^[a-z0-9_.]{3,20}$/.test(un)) return err("اسم المستخدم: من ٣ لـ ٢٠ حرف إنجليزي أو رقم، من غير مسافات");
          if (U[un]) return err("اسم المستخدم ده موجود قبل كده، اختار اسم تاني");
          if (pw.length < 4) return err("كلمة السر لازم تكون ٤ حروف أو أرقام على الأقل");
          if (pw !== val("pass2")) return err("كلمة السر وتأكيدها مش زي بعض");
          U[un] = { name, pass: hashPw(pw), created: Date.now() };
          store.set("users", U);
          close(); session = un; store.set("name", name); setSession(un);
          sfx.win(); toast(`تم إنشاء حسابك — أهلًا يا ${firstName()}`);
        }
      };
      show(tab);
    });
  }

  function accountModal() {
    sfx.click();
    const u = currentUser();
    modal(`<div class="big-ico">${I("user")}</div><h3>أهلًا يا ${esc(u.name)}</h3>
      <p style="color:var(--muted)">اسم المستخدم: <b dir="ltr">${esc(session)}</b><br>تقدّمك ودرجاتك محفوظين على حسابك.</p>
      <div class="btn-row" style="justify-content:center"><button class="btn" data-out>${I("logout")} تسجيل الخروج</button><button class="btn soft" data-n>إغلاق</button></div>`,
      (m, close) => {
        m.querySelector("[data-n]").onclick = close;
        m.querySelector("[data-out]").onclick = () => { close(); setSession(null); toast("تم تسجيل الخروج"); };
      });
  }

  /* ---------------- الشريط العلوي ---------------- */
  const $user = document.getElementById("btn-user"), $userLbl = document.getElementById("user-label");
  const openUser = () => (currentUser() ? accountModal() : authModal("login"));
  $user.onclick = openUser;
  $user.onkeydown = (e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); openUser(); } };
  if (session && !currentUser()) { session = null; store.del("user"); }
  paintUser();
  document.getElementById("brand-name").textContent = SITE.name;
  document.getElementById("brand-cup").innerHTML = cupSVG("cup");
  const $ex = document.getElementById("btn-exit");
  $ex.innerHTML = `${I("power")}<span class="lbl"> خروج</span>`;
  const goHome = () => { sfx.click(); history.length = 0; go("home", {}, false); };
  document.querySelector(".brand").onclick = goHome;

  /* ---------------- قائمة المنصة ---------------- */
  $$(".nav-btn", $navBox).forEach((b) => {
    b.onmouseenter = () => outlineTo(b);
    b.onfocus = () => outlineTo(b);
    b.onclick = () => {
      const k = b.dataset.nav;
      if (k === "home") goHome();
      if (k === "lessons") {
        if (!current || current.name !== "home") goHome(); else sfx.click();
        const u = $("[data-units]"); if (u) u.scrollIntoView({ behavior: "smooth", block: "start" });
        setNav("lessons");
      }
    };
  });
  $navBox.onmouseleave = () => outlineTo($(".nav-btn.active", $navBox));
  window.addEventListener("resize", () => outlineTo($(".nav-btn.active", $navBox)));
  $ex.onclick = askExit;
  // زرار الوضع الليلي (الشمس والقمر): لما يتعلّم عليه يبقى ليلي
  const $th = document.getElementById("theme-check");
  const applyTheme = (t) => { document.documentElement.dataset.theme = t; $th.checked = t === "dark"; };
  applyTheme(store.get("theme", "light"));
  $th.onchange = () => { const t = $th.checked ? "dark" : "light"; store.set("theme", t); applyTheme(t); sfx.click(); };
  document.addEventListener("keydown", (e) => { if (e.key === "Escape" && current && current.name !== "bye" && !document.querySelector(".modal-bg")) askExit(); });
  document.title = SITE.name;

  go("home", {}, false);

  /* ---------------- شاشة التحميل: ٢.٥ ثانية وبعدين تختفي ---------------- */
  const $loader = document.getElementById("loader");
  if ($loader) {
    $(".loader-title", $loader).textContent = SITE.name;
    setTimeout(() => { $loader.classList.add("done"); setTimeout(() => $loader.remove(), 600); }, 2500);
  }
})();
